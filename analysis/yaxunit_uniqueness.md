# Уникальность проекта YAxUnit

**Репозиторий:** [bia-technologies/yaxunit](https://github.com/bia-technologies/yaxunit)

---

## 🎯 Что делает YAxUnit уникальным

YAxUnit — это не просто еще один фреймворк для тестирования в 1С. Это **современная, продуманная экосистема** с уникальными возможностями, которых нет в других решениях для тестирования 1С.

---

## 1. 🔗 Fluent API (Цепочечный синтаксис)

### Уникальность
**YAxUnit — единственный фреймворк в экосистеме 1С с полноценным Fluent API**, который делает тесты читаемыми как обычный текст.

### Как это работает
```bsl
// Читается как обычное предложение на русском языке
ЮТест.ОжидаетЧто(Результат)
    .ЭтоИстина()
    .ИмеетТип("Число")
    .Равно(42)
    .ИмеетПредставление("Результат вычисления");
```

### Почему это уникально
- **xUnitFor1C** — использует классический подход с отдельными функциями
- **vanessa-automation** — фокус на UI-тестировании, не на unit-тестах
- **YAxUnit** — единственный с Fluent API, который делает код самодокументируемым

### Преимущества
- ✅ Тесты читаются как документация
- ✅ Не нужно запоминать названия функций
- ✅ Автодополнение в IDE подсказывает следующие шаги
- ✅ Меньше ошибок при написании тестов

---

## 2. 🎭 Мокито (Mockito-подобная система моков)

### Уникальность
**YAxUnit — единственный фреймворк в 1С с полноценной системой моков**, включающей три режима работы: Обучение, Прогон, Проверка.

### Как это работает
```bsl
// 1. ОБУЧЕНИЕ - настраиваем поведение мока
Мокито.Обучение(МойОбъект)
    .Когда(МойОбъект.ПолучитьДанные(Мокито.ЛюбойПараметр()))
    .ТогдаВернуть(ТестовыеДанные);

// 2. ПРОГОН - переводим в режим тестирования
Мокито.Прогон();

// 3. ВЫПОЛНЕНИЕ - используем мок в тесте
Результат = МойМодуль.Обработать(МойОбъект);

// 4. ПРОВЕРКА - верифицируем вызовы
Мокито.Проверить(МойОбъект)
    .Метод("ПолучитьДанные")
    .БылВызван(1);
```

### Уникальные возможности
1. **Три режима работы:**
   - `Обучение` — настройка поведения мока
   - `Прогон` — выполнение теста
   - `Проверка` — верификация вызовов

2. **Гибкая фильтрация параметров:**
   ```bsl
   Мокито.Когда(Объект.Метод(
       Мокито.ЛюбойПараметр(),        // любой параметр
       Мокито.ЧисловойПараметр(),     // только число
       Мокито.ТипизированныйПараметр(Тип("Строка"))  // конкретный тип
   ))
   ```

3. **Статистика вызовов:**
   - Подсчет количества вызовов
   - Проверка параметров вызовов
   - Верификация порядка вызовов

### Почему это уникально
- **xUnitFor1C** — нет системы моков
- **vanessa-automation** — фокус на UI, не на моках
- **YAxUnit** — единственный с полноценной системой моков, сравнимой с Mockito в Java

---

## 3. 🔍 Система предикатов

### Уникальность
**YAxUnit — единственный фреймворк с системой предикатов** для фильтрации и проверки коллекций с тем же синтаксисом, что и утверждения.

### Как это работает
```bsl
// Предикат можно использовать для фильтрации коллекций
ЮТест.ОжидаетЧто(МассивДокументов)
    .СодержитЭлемент(ЮТест.Предикат()
        .Реквизит("Сумма").Больше(1000)
        .И().Реквизит("Статус").Равно("Проведен")
    );

// Или для проверки всех элементов
ЮТест.ОжидаетЧто(МассивДокументов)
    .ВсеЭлементы(ЮТест.Предикат()
        .Реквизит("Статус").Заполнено()
    );
```

### Уникальные возможности
1. **Единый синтаксис** — предикаты используют тот же Fluent API, что и утверждения
2. **Композиция условий** — можно комбинировать с `.И()`, `.Или()`, `.Не()`
3. **Гибкая навигация** — доступ к вложенным свойствам через точку: `Реквизит("Контрагент.Наименование")`
4. **Работа с индексами** — `Реквизит(0)` для массивов

### Почему это уникально
- Другие фреймворки требуют писать циклы и условия вручную
- YAxUnit позволяет описывать проверки декларативно

---

## 4. 🎲 Богатые генераторы тестовых данных

### Уникальность
**YAxUnit имеет самый богатый набор генераторов тестовых данных** среди всех фреймворков для 1С.

### Что можно генерировать

#### Базовые типы
```bsl
Данные().СлучайноеЧисло(1, 100)
Данные().СлучайнаяСтрока(10)
Данные().СлучайнаяДата()
Данные().СлучайныйUUID()
```

#### Российские специфичные данные
```bsl
Данные().СлучайныйИНН()              // Валидный ИНН
Данные().СлучайныйСНИЛС()            // Валидный СНИЛС
Данные().СлучайныйНомерТелефона()     // +7(XXX)XXX-XX-XX
Данные().СлучайныйIPАдрес()          // XXX.XXX.XXX.XXX
```

#### Объекты метаданных
```bsl
Данные().Справочник("Справочник.Пользователи")
Данные().Документ("Документ.Реализация")
Данные().СоздатьЭлемент(Менеджер, "Наименование", Реквизиты)
```

#### Коллекции
```bsl
Данные().Массив(10, "Строка")        // Массив из 10 строк
Данные().Структура("Ключ1, Ключ2", Значение1, Значение2)
Данные().Соответствие(МассивКлючей, МассивЗначений)
```

### Уникальные возможности
1. **Валидность данных** — все генераторы создают валидные данные (ИНН, СНИЛС проходят проверку)
2. **Автоматическое заполнение** — умное заполнение реквизитов объектов
3. **Поддержка английского языка** — автоматическое определение языка платформы
4. **Гибкая настройка** — можно задавать параметры генерации

### Почему это уникально
- Другие фреймворки требуют вручную создавать тестовые данные
- YAxUnit предоставляет готовые генераторы для всех типов данных

---

## 5. 🗄️ Проверка базы данных

### Уникальность
**YAxUnit — единственный фреймворк с специальными утверждениями для проверки данных в базе**, без необходимости писать запросы вручную.

### Как это работает
```bsl
// Проверка количества записей
ЮТест.ОжидаетЧтоТаблицаБазы("Справочник.Пользователи")
    .СодержитЗаписей(10);

// Проверка с условием
ЮТест.ОжидаетЧтоТаблицаБазы("Документ.Реализация")
    .Где("Дата = &Дата", ТекущаяДата())
    .СодержитЗаписей(5);

// Проверка наличия записи
ЮТест.ОжидаетЧтоТаблицаБазы("Справочник.Контрагенты")
    .СодержитЗапись(ЮТест.Предикат()
        .Реквизит("ИНН").Равно("1234567890")
    );
```

### Уникальные возможности
1. **Декларативный синтаксис** — не нужно писать запросы
2. **Интеграция с предикатами** — можно использовать предикаты для фильтрации
3. **Проверка связей** — автоматическая проверка связей между объектами
4. **Оптимизация** — автоматическая оптимизация запросов

### Почему это уникально
- Другие фреймворки требуют писать запросы вручную
- YAxUnit предоставляет высокоуровневый API для работы с БД

---

## 6. 📦 Трехуровневая система контекстов

### Уникальность
**YAxUnit — единственный фреймворк с трехуровневой системой контекстов** для управления данными тестов на разных уровнях.

### Как это работает
```bsl
// Уровень 1: Контекст теста (живет один тест)
Процедура Тест1() Экспорт
    КонтекстТеста().Вставить("Данные", СозданныеДанные);
    // Данные доступны только в этом тесте
КонецПроцедуры

// Уровень 2: Контекст набора тестов (живет набор тестов)
Процедура ПередТестовымНабором() Экспорт
    КонтекстТестовогоНабора().Вставить("ОбщиеДанные", Данные);
    // Данные доступны во всех тестах набора
КонецПроцедуры

// Уровень 3: Контекст модуля (живет весь модуль)
Процедура ПередВсемиТестами() Экспорт
    КонтекстМодуля().Вставить("ГлобальныеДанные", Данные);
    // Данные доступны во всех тестах модуля
КонецПроцедуры

// Умный контекст - автоматически ищет во всех уровнях
ЮТест.Контекст().Получить("Данные");  // Ищет в тесте -> наборе -> модуле
```

### Уникальные возможности
1. **Автоматический поиск** — `Контекст()` автоматически ищет значение во всех уровнях
2. **Изоляция данных** — данные теста не влияют на другие тесты
3. **Переиспользование** — общие данные можно хранить на уровне набора или модуля
4. **Автоматическая очистка** — контексты очищаются автоматически

### Почему это уникально
- Другие фреймворки используют глобальные переменные или структуры
- YAxUnit предоставляет структурированную систему управления данными

---

## 7. 🎯 Параметризованные тесты (Варианты)

### Уникальность
**YAxUnit поддерживает параметризованные тесты** через систему вариантов, что позволяет запускать один тест с разными параметрами.

### Как это работает
```bsl
Процедура ТестСРазнымиПараметрами() Экспорт
    
    Варианты = ЮТест.Варианты("Число1, Число2, ОжидаемыйРезультат");
    Варианты.Добавить(Новый Структура("Число1, Число2, ОжидаемыйРезультат", 2, 2, 4));
    Варианты.Добавить(Новый Структура("Число1, Число2, ОжидаемыйРезультат", 3, 3, 6));
    Варианты.Добавить(Новый Структура("Число1, Число2, ОжидаемыйРезультат", 5, 5, 10));
    
    Для Каждого Вариант Из Варианты Цикл
        Результат = Вариант.Число1 + Вариант.Число2;
        ЮТест.ОжидаетЧто(Результат).Равно(Вариант.ОжидаемыйРезультат);
    КонецЦикла;
    
КонецПроцедуры
```

### Уникальные возможности
1. **Декларативное описание** — варианты описываются структурами
2. **Автоматический прогон** — каждый вариант запускается отдельно
3. **Отчетность** — в отчете видно результаты каждого варианта
4. **Гибкость** — можно использовать любые параметры

### Почему это уникально
- Другие фреймворки требуют писать отдельные тесты для каждого варианта
- YAxUnit позволяет описать варианты декларативно

---

## 8. 🎨 Красивый UI для отчетов

### Уникальность
**YAxUnit имеет современный веб-интерфейс** для просмотра результатов тестов, чего нет в других фреймворках.

### Возможности UI
- 📊 Визуализация результатов тестов
- 🎯 Группировка по модулям и наборам
- ⏱️ Время выполнения тестов
- 📝 Детальные логи выполнения
- 🔍 Фильтрация и поиск
- 📈 Статистика покрытия

### Почему это уникально
- Другие фреймворки выводят результаты в консоль или файлы
- YAxUnit предоставляет интерактивный веб-интерфейс

---

## 9. 🌐 Поддержка разных локалей

### Уникальность
**YAxUnit автоматически адаптируется под язык платформы** (русский/английский), что уникально для фреймворков 1С.

### Как это работает
```bsl
// Автоматически использует правильное имя реквизита
Данные().СоздатьЭлемент(Менеджер, "Имя");
// В русской версии: "Наименование"
// В английской версии: "Description"
```

### Почему это уникально
- Другие фреймворки требуют явно указывать имена реквизитов
- YAxUnit автоматически определяет язык и использует правильные имена

---

## 10. 🔧 Расширяемость и плагинность

### Уникальность
**YAxUnit спроектирован с учетом расширяемости** — можно добавлять свои утверждения, генераторы данных, моки.

### Возможности расширения
1. **Кастомные утверждения** — можно создать свои методы утверждений
2. **Кастомные генераторы** — можно добавить свои генераторы данных
3. **Кастомные моки** — можно расширить систему моков
4. **События** — можно подписываться на события жизненного цикла

### Почему это уникально
- Другие фреймворки имеют фиксированный набор возможностей
- YAxUnit позволяет расширять функциональность под свои нужды

---

## 📊 Сравнение с другими решениями

| Возможность | YAxUnit | xUnitFor1C | vanessa-automation |
|------------|---------|------------|-------------------|
| Fluent API | ✅ | ❌ | ❌ |
| Система моков | ✅ Полная | ❌ | ❌ |
| Предикаты | ✅ | ❌ | ❌ |
| Генераторы данных | ✅ Богатые | ⚠️ Базовые | ⚠️ Базовые |
| Проверка БД | ✅ Специальный API | ⚠️ Через запросы | ⚠️ Через запросы |
| Контексты | ✅ Трехуровневые | ⚠️ Глобальные | ⚠️ Глобальные |
| Параметризованные тесты | ✅ | ⚠️ Ограниченно | ❌ |
| Веб-интерфейс | ✅ | ❌ | ⚠️ Базовый |
| Поддержка локалей | ✅ Автоматическая | ❌ | ❌ |
| Расширяемость | ✅ Высокая | ⚠️ Средняя | ⚠️ Средняя |
| Документация | ✅ Отличная | ⚠️ Базовая | ⚠️ Базовая |

---

## 🎯 Итоговая уникальность

YAxUnit уникален тем, что это **не просто фреймворк для тестирования, а полноценная экосистема** с:

1. ✅ **Современным синтаксисом** (Fluent API)
2. ✅ **Полноценной системой моков** (как Mockito)
3. ✅ **Декларативными проверками** (предикаты, БД)
4. ✅ **Богатыми генераторами данных**
5. ✅ **Умной системой контекстов**
6. ✅ **Современным UI**
7. ✅ **Автоматической адаптацией** под язык платформы
8. ✅ **Высокой расширяемостью**

**Ни один другой фреймворк для тестирования 1С не предоставляет такой комплексный набор возможностей.**

---

**Источники:**
- [GitHub Repository](https://github.com/bia-technologies/yaxunit)
- [Документация](https://bia-technologies.github.io/yaxunit/)
- Анализ исходного кода проекта

