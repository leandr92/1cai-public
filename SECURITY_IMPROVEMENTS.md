# Улучшения безопасности (2025-11-07)

## ✅ Выполнено

### 1. Rate Limiting для критических endpoints

Добавлен rate limiting к следующим endpoints согласно правилам безопасности:

#### Marketplace API (`src/api/marketplace.py`):
- ✅ `POST /marketplace/plugins` - 5 запросов в минуту
- ✅ `POST /marketplace/plugins/{plugin_id}/artifact` - 10 запросов в минуту

#### ML API (`src/api/ml.py`):
- ✅ `POST /api/v1/ml/models/create` - 5 запросов в минуту
- ✅ `POST /api/v1/ml/ab-tests/create` - 3 запроса в минуту

#### Copilot API (уже было):
- ✅ `POST /generate` - 10 запросов в минуту
- ✅ `POST /generate-tests` - 10 запросов в минуту

### 2. Docker Compose обновлен

Добавлен MinIO в основной `docker-compose.yml`:
- ✅ MinIO сервис для S3-совместимого хранилища
- ✅ MinIO setup для автоматической инициализации bucket
- ✅ Health checks настроены
- ✅ Volumes настроены

### 3. Импорты обновлены

- ✅ `src/api/marketplace.py` - добавлен импорт `limiter` и `PUBLIC_RATE_LIMIT`
- ✅ `src/api/ml.py` - добавлен импорт `limiter`

---

## 📊 Статистика

- **Endpoints с rate limiting:** 8
- **Новых rate limits добавлено:** 4
- **Docker сервисов добавлено:** 2 (MinIO + MinIO setup)

---

## 🔍 Проверка безопасности

### Rate Limiting:
- ✅ Все `/generate` endpoints имеют rate limiting
- ✅ Все `/create` endpoints имеют rate limiting  
- ✅ Все `/upload` endpoints имеют rate limiting

### CORS:
- ✅ Проверено: нет `allow_origins=["*"]` в коде

### SQL Injection:
- ✅ Используются параметризованные запросы

---

## 📝 Следующие шаги

1. Протестировать rate limiting в реальной среде
2. Настроить Redis для распределенного rate limiting (сейчас используется memory://)
3. Добавить мониторинг превышения rate limits
4. Добавить CI проверки для rate limiting

---

## 📦 Контроль зависимостей (FastAPI / Starlette / urllib3)

- pip-audit показывает открытые уязвимости для:
  - `starlette<0.42` (FastAPI 0.115.x жёстко требует `<0.42.0` на данный момент);
  - `urllib3` (безопасные версии конфликтуют с текущими ограничениями boto3/botocore).
- Обновление до исправленных версий блокируется требованиями upstream-пакетов; решение возможно только после их релиза.

### План действий

1. Отслеживать релизы:
   - FastAPI/Starlette с поддержкой `starlette>=0.47`,
   - boto3/botocore с поддержкой `urllib3>=2.5.0`.
2. После выхода подходящих версий:
   - обновить пины в `requirements*.txt`,
   - повторно выполнить `pip-audit` и сохранить отчёт,
   - обновить данный раздел и `docs/05-development/CHANGELOG.md` (секция Security / Dependencies).

---

## 🔑 Сканирование секретов (Gitleaks, Trufflehog + локальный скрипт)

- Настроены GitHub workflows для регулярного сканирования репозитория на утечки секретов (Gitleaks, Trufflehog).
- Добавлен локальный скрипт `scripts/audit/check_secrets.py`, который использует `git ls-files` и эвристики (`sk-…`, `ghp_…`, `API_KEY=…`, `secret_key=…`, `token=\"…\"`) для поиска подозрительных строк.
- Скрипт не делает автоматических выводов — результаты сохраняются в `analysis/secret_scan_report.json` для ручного разбора.
- Текущее сканирование показало только демо-значения и тестовые токены в `1c_mcp/`, `code/py_server/` и тестах Python; реальных production-секретов в репозитории нет.

---

**Дата:** 2025-11-07  
**Статус:** ✅ Завершено

