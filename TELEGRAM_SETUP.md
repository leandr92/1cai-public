# 🚀 Telegram Bot - Полная установка

## Что получится в итоге:

✅ Telegram бот для 1С разработчиков
✅ Семантический поиск по коду
✅ Генерация BSL кода
✅ Анализ зависимостей
✅ Интеграция с Neo4j, Qdrant, PostgreSQL
✅ Rate limiting и админ-панель
✅ Готово к production

---

## 📋 Пошаговая инструкция

### ШАГ 1: Создайте Telegram бота (5 минут)

1. Откройте Telegram → найдите **@BotFather**
2. Отправьте `/newbot`
3. Введите название: `My 1C AI Assistant`
4. Username (должен заканчиваться на `_bot`): `my_1c_ai_bot`
5. **СКОПИРУЙТЕ ТОКЕН!** Выглядит так:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

6. Настройте описание бота:
   ```
   /setdescription
   [выберите бота]
   AI-помощник для 1С разработчиков. Семантический поиск, генерация кода, анализ зависимостей.
   ```

7. Настройте команды в меню:
   ```
   /setcommands
   [выберите бота]
   
   start - Начать работу
   search - Поиск кода
   generate - Генерация BSL кода
   deps - Анализ зависимостей
   stats - Статистика
   premium - Premium подписка
   help - Справка
   ```

### ШАГ 2: Настройте окружение

1. Скопируйте example файл:
   ```bash
   cp ENV_EXAMPLE.txt .env
   ```

2. Откройте `.env` в редакторе:
   ```bash
   nano .env  # или любой редактор
   ```

3. Заполните обязательные поля:
   ```bash
   # Токен от BotFather (ОБЯЗАТЕЛЬНО!)
   TELEGRAM_BOT_TOKEN=ваш_токен_здесь
   
   # Пароли для БД
   POSTGRES_PASSWORD=придумайте_сильный_пароль
   NEO4J_PASSWORD=другой_сильный_пароль
   ```

4. Узнайте свой Telegram ID (для админ прав):
   - Откройте [@userinfobot](https://t.me/userinfobot)
   - Скопируйте свой ID (например: 123456789)
   - Добавьте в `.env`:
     ```bash
     TELEGRAM_ADMIN_IDS=123456789
     ```

### ШАГ 3: Запустите инфраструктуру

```bash
# Запуск всех сервисов (PostgreSQL, Neo4j, Qdrant, etc)
docker-compose -f docker-compose.yml -f docker-compose.stage1.yml up -d

# Проверка статуса (должны быть healthy)
docker ps
```

**Ожидайте 1-2 минуты пока все сервисы запустятся**

### ШАГ 4: Запустите Telegram бота

**Вариант A: Docker (рекомендуется для production)**

```bash
# Запуск с профилем telegram
docker-compose -f docker-compose.yml \
               -f docker-compose.stage1.yml \
               --profile telegram up -d

# Проверьте логи
docker logs -f 1c-ai-telegram-bot
```

**Вариант B: Локально (для разработки)**

```bash
# Установите зависимости
pip install -r requirements.txt
pip install -r requirements-telegram.txt

# Запустите
python -m src.telegram.bot
```

Должны увидеть:
```
✅ Bot started: @my_1c_ai_bot
🔄 Starting polling...
```

### ШАГ 5: Проверьте работу

1. Откройте Telegram
2. Найдите своего бота: `@my_1c_ai_bot`
3. Нажмите **Start** или отправьте `/start`
4. Должны получить приветственное сообщение! 🎉

### ШАГ 6: Протестируйте команды

```
/help
→ Список всех команд

/search тестовый запрос
→ Семантический поиск (должен работать даже если БД пустая)

/stats
→ Ваша статистика использования

/premium
→ Информация о Premium подписке
```

---

## 🎯 Что делать дальше?

### Для разработки:

1. **Добавьте тестовые данные** в БД:
   ```bash
   # Запустите скрипты миграции (если есть данные)
   python scripts/migrate_json_to_postgres.py
   python scripts/migrate_postgres_to_neo4j.py
   python scripts/migrate_to_qdrant.py
   ```

2. **Настройте IDE интеграцию:**
   - Cursor: MCP сервер уже работает на `/mcp`
   - VSCode: установите расширение

### Для запуска:

1. **Пригласите первых пользователей:**
   - Коллеги по работе
   - Друзья-разработчики
   - 1С сообщество

2. **Соберите feedback:**
   - Что работает?
   - Что не работает?
   - Какие фичи нужны?

3. **Следуйте Distribution Strategy:**
   См. [docs/TELEGRAM_DISTRIBUTION_STRATEGY.md](docs/TELEGRAM_DISTRIBUTION_STRATEGY.md)

---

## 📊 Мониторинг

### Логи

**Docker:**
```bash
# Все сервисы
docker-compose logs -f

# Только бот
docker logs -f 1c-ai-telegram-bot

# Поиск ошибок
docker logs 1c-ai-telegram-bot | grep ERROR
```

**Локально:**
Логи выводятся в консоль

### Метрики

Добавьте в бота команду для админов:

```python
@router.message(Command("admin_stats"))
async def admin_stats(message: Message):
    if message.from_user.id not in config.admin_ids:
        return
    
    stats = {
        "total_users": get_total_users(),
        "active_today": get_active_today(),
        "total_requests": get_total_requests(),
    }
    
    await message.reply(f"📊 Admin Stats:\n{stats}")
```

---

## 🐛 Troubleshooting

### Проблема: Бот не отвечает

**Решение 1:** Проверьте токен
```bash
grep TELEGRAM_BOT_TOKEN .env
# Должен быть заполнен и без пробелов
```

**Решение 2:** Проверьте логи
```bash
docker logs 1c-ai-telegram-bot
```

**Решение 3:** Перезапустите бота
```bash
docker restart 1c-ai-telegram-bot
```

### Проблема: Connection refused к БД

**Проверьте что все сервисы запущены:**
```bash
docker ps | grep 1c-ai
```

Должны быть:
- `1c-ai-postgres` (healthy)
- `1c-ai-neo4j` (healthy)
- `1c-ai-qdrant` (healthy)
- `1c-ai-redis` (up)

**Если что-то не запустилось:**
```bash
docker-compose logs <service-name>
# Например: docker-compose logs neo4j
```

### Проблема: Rate limit срабатывает сразу

**Увеличьте лимиты в .env:**
```bash
TELEGRAM_RATE_LIMIT_MIN=50
TELEGRAM_RATE_LIMIT_DAY=1000
```

**Перезапустите бота:**
```bash
docker restart 1c-ai-telegram-bot
```

### Проблема: "Conflict: terminated by other getUpdates"

**Бот уже запущен где-то еще. Остановите все инстансы:**
```bash
# Docker
docker stop 1c-ai-telegram-bot

# Локально - закройте терминал или Ctrl+C

# Подождите 30 секунд и запустите снова
```

---

## 🔒 Безопасность

### Best practices:

1. ✅ Храните `.env` в gitignore (уже добавлено)
2. ✅ Используйте сильные пароли для БД
3. ✅ Rate limiting защищает от спама (настроен)
4. ✅ Admin ID - только вы видите системные команды
5. ✅ Валидация входных данных (реализовано)

### Production checklist:

- [ ] Смените пароли БД на сильные
- [ ] Настройте webhook вместо polling
- [ ] Добавьте SSL сертификат
- [ ] Настройте backup БД
- [ ] Подключите мониторинг (Prometheus/Grafana)
- [ ] Настройте alerts на ошибки
- [ ] Ограничьте доступ к портам (firewall)

---

## 📚 Документация

**Для пользователей:**
- [TELEGRAM_README.md](docs/TELEGRAM_README.md) - Обзор возможностей
- [TELEGRAM_BOT_QUICKSTART.md](docs/TELEGRAM_BOT_QUICKSTART.md) - Быстрый старт

**Для разработчиков:**
- [src/telegram/](src/telegram/) - Исходный код
- [TELEGRAM_DISTRIBUTION_STRATEGY.md](docs/TELEGRAM_DISTRIBUTION_STRATEGY.md) - Стратегия распространения

**Техническое:**
- [docker-compose.stage1.yml](docker-compose.stage1.yml) - Конфигурация Docker
- [requirements-telegram.txt](requirements-telegram.txt) - Python зависимости

---

## 💡 Советы

### Для лучшего UX:

1. **Настройте аватар бота:**
   - @BotFather → `/setuserpic`
   - Загрузите логотип 512x512px

2. **Добавьте about:**
   - @BotFather → `/setabouttext`
   - "AI для 1С разработчиков"

3. **Inline mode (опционально):**
   - Позволяет вызывать бота в любом чате: `@your_bot query`
   - @BotFather → `/setinline`

### Для роста:

1. **Создайте канал:** [@your_bot_news](https://t.me/your_bot_news)
2. **Пост раз в неделю:** новые фичи, кейсы, статистика
3. **Реферальная программа:** друзья приглашают друзей
4. **Партнерства:** найдите 1С блогеров/инфлюенсеров

---

## ✅ Checklist готовности

**Технически:**
- [ ] Бот отвечает на `/start`
- [ ] Все команды работают
- [ ] БД подключены
- [ ] Rate limiting работает
- [ ] Логирование настроено
- [ ] Docker образы собраны

**Контентно:**
- [ ] Описание бота заполнено
- [ ] Команды настроены в меню
- [ ] Аватар загружен
- [ ] Канал новостей создан
- [ ] Первый пост готов

**Запуск:**
- [ ] 5 друзей протестировали
- [ ] Feedback собран
- [ ] Критичные баги исправлены
- [ ] Готов к публичному запуску

---

## 🚀 ЗАПУСК!

**Когда всё готово:**

1. Напишите первый пост в канале
2. Отправьте в 3-5 тематических чатов (с разрешения админов)
3. Мониторьте feedback первые 24 часа
4. Итерируйте быстро

**Цель первой недели:** 100 пользователей + 10 активных

**Удачи! 🎉**

---

## 🆘 Поддержка

**Проблемы?**
- GitHub Issues: [создать issue](../../issues)
- Telegram: @ai1c_support (создайте канал поддержки)
- Email: support@your-domain.com

**Хотите помочь проекту?**
- ⭐ Star на GitHub
- 🐛 Сообщайте о багах
- 💡 Предлагайте фичи
- 📝 Улучшайте документацию
- 🔧 Делайте Pull Requests

---

**Документ обновлен:** 2025-11-04
**Версия:** 1.0.0


