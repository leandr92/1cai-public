# Система мониторинга

Полнофункциональная система мониторинга с использованием Prometheus, Grafana, AlertManager, ELK Stack и Traefik.

## 🚀 Компоненты системы

### Мониторинг и Метрики
- **Prometheus** - сбор и хранение метрик
- **AlertManager** - управление алертами и уведомлениями  
- **Grafana** - визуализация дашбордов
- **Traefik** - обратный прокси и маршрутизация

### Логирование
- **Elasticsearch** - поиск и анализ логов
- **Kibana** - визуализация логов
- **Filebeat** - сбор логов из контейнеров и файлов

### Экспортеры метрик
- **Node Exporter** - системные метрики серверов
- **cAdvisor** - метрики Docker контейнеров
- **PostgreSQL Exporter** - метрики базы данных
- **Redis Exporter** - метрики Redis
- **Elasticsearch Exporter** - метрики Elasticsearch
- **Kafka Exporter** - метрики Kafka
- **Nginx Exporter** - метрики Nginx

### Отслеживание
- **Jaeger** - распределенное отслеживание запросов

## 📊 Дашборды

### Доступные дашборды в Grafana:
1. **Системный мониторинг** - общий статус системы, ресурсы, алерты
2. **HTTP метрики** - запросы, время ответа, ошибки
3. **База данных** - производительность PostgreSQL
4. **Redis** - память, операции, производительность
5. **Контейнеры** - использование ресурсов Docker

## 🔔 Алерты и Уведомления

### Уровни алертов:
- **Critical** - критичные проблемы (сервис не работает, высокая нагрузка)
- **Warning** - предупреждения (высокое использование ресурсов)
- **Info** - информационные уведомления

### Каналы уведомлений:
- **Slack** - каналы команд
- **Email** - оповещения командам
- **Telegram** - мгновенные уведомления
- **Webhook** - интеграции с внешними системами

## 🏗️ Установка и настройка

### 1. Предварительные требования
```bash
# Docker и Docker Compose
sudo apt update
sudo apt install docker.io docker-compose

# Для Ubuntu/Debian систем
sudo apt install curl gnupg2 software-properties-common
```

### 2. Клонирование и запуск
```bash
# Копирование конфигураций
cp -r monitoring/ /opt/monitoring/

# Переход в директорию
cd /opt/monitoring/docker

# Создание директорий
sudo mkdir -p {prometheus_data,grafana_data,alertmanager_data,elasticsearch_data,kibana_data,filebeat_data}

# Установка прав доступа
sudo chown -R 1000:1000 grafana_data kibana_data elasticsearch_data
sudo chown -R 472:472 filebeat_data

# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### 3. Настройка паролей (измените значения по умолчанию!)
```bash
# Создание секретного файла
cat > .env << EOF
ELASTIC_PASSWORD=your_secure_password_here
GRAFANA_PASSWORD=your_secure_password_here
ALERTMANAGER_WEBHOOK_SECRET=your_webhook_secret_here
EOF
```

### 4. Обновление конфигураций
```bash
# Обновите AlertManager настройки уведомлений
nano alertmanager/alertmanager.yml

# Обновите Slack/Telegram токены
# Обновите email настройки
```

## 🔍 Проверка системы

### Доступные интерфейсы:
- **Grafana**: http://localhost:3000 (admin/admin123)
- **Prometheus**: http://localhost:9090
- **AlertManager**: http://localhost:9093
- **Kibana**: http://localhost:5601
- **Traefik Dashboard**: http://localhost:8080

### Проверка health check endpoints:
```bash
# Статус системы
curl http://localhost:8080/health

# Готовность сервиса
curl http://localhost:8080/ready

# Prometheus метрики
curl http://localhost:8080/metrics
```

## ⚙️ Конфигурация экспортеров

### Настройка внешних сервисов:
Обновите конфигурации экспортеров в папке `exporters/`:

```yaml
# PostgreSQL exporter
data-source-names:
  - "postgresql://username:password@host:5432/dbname?sslmode=disable"

# Redis exporter
redis.addr: "redis://redis-host:6379"
redis.password: "your_redis_password"
```

### Добавление новых сервисов:
1. Добавьте в `prometheus/prometheus.yml`
2. Создайте health check middleware
3. Обновите дашборды в Grafana

## 📈 Мониторинг производительности

### Рекомендуемые пороги алертов:
- **CPU**: > 80% на 5 минут
- **Память**: > 85% на 5 минут  
- **Диск**: > 85% использования
- **HTTP ошибки**: > 5% от общего количества
- **Время ответа**: > 1 секунды (95-й перцентиль)

### Метрики производительности:
- **Пропускная способность**: запросы в секунду
- **Задержка**: среднее время ответа
- **Ошибки**: процент неудачных запросов
- **Доступность**: uptime сервисов

## 🔧 Устранение неисправностей

### Проверка логов:
```bash
# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f prometheus
docker-compose logs -f grafana
docker-compose logs -f elasticsearch

# Логи Filebeat
docker-compose logs -f filebeat
```

### Проблемы с алертами:
```bash
# Проверка статуса Prometheus
curl http://localhost:9090/-/healthy

# Проверка AlertManager
curl http://localhost:9093/-/healthy

# Проверка правил алертов
curl http://localhost:9090/api/v1/rules
```

### Проблемы с Elasticsearch:
```bash
# Проверка статуса кластера
curl -u elastic:changeme http://localhost:9200/_cluster/health

# Индексы
curl -u elastic:changeme http://localhost:9200/_cat/indices?v
```

## 🔐 Безопасность

### Настройка TLS:
1. Получите SSL сертификаты
2. Обновите Traefik конфигурацию
3. Включите Let's Encrypt в production

### Защита интерфейсов:
- Измените пароли по умолчанию
- Настройте базовую аутентификацию
- Ограничьте доступ к админ панелям
- Используйте VPN для доступа к мониторингу

### Обновление зависимостей:
```bash
# Обновление образов
docker-compose pull
docker-compose up -d

# Проверка уязвимостей
docker run --rm -v /var/run/docker.sock:/var/run/docker.sock \
  aquasec/trivy image prom/prometheus:latest
```

## 📊 Настройка дашбордов

### Создание нового дашборда:
1. Войдите в Grafana
2. Создайте новый дашборд
3. Добавьте панели с запросами PromQL
4. Настройте алерты при необходимости

### Примеры запросов:
```promql
# Использование CPU
100 - (avg by(instance) (rate(node_cpu_seconds_total{mode="idle"}[5m])) * 100)

# Использование памяти
(1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) * 100

# HTTP запросы
sum(rate(http_requests_total[5m])) by (job)

# Время ответа
histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))
```

## 🚀 Масштабирование

### Добавление узлов:
1. Скопируйте Node Exporter на новый сервер
2. Обновите Prometheus конфигурацию
3. Добавьте новые метрики в Grafana

### Высокая доступность:
1. Настройте несколько экземпляров Prometheus
2. Используйте Thanos или Cortex для горизонтального масштабирования
3. Настройте AlertManager в кластерном режиме

## 📝 Дополнительные ресурсы

- [Prometheus документация](https://prometheus.io/docs/)
- [Grafana документация](https://grafana.com/docs/)
- [AlertManager документация](https://prometheus.io/docs/alerting/latest/alertmanager/)
- [ELK Stack документация](https://www.elastic.co/guide/)
- [Traefik документация](https://doc.traefik.io/traefik/)

## 🐛 Сообщение об ошибках

При обнаружении проблем:
1. Проверьте логи сервисов
2. Проверьте статус сервисов Docker
3. Создайте issue с описанием проблемы и логами
