# Scripts Documentation

Данная директория содержит вспомогательные скрипты для разработки, тестирования и развертывания проекта.

## 📁 Структура

```
scripts/
├── README.md           # Этот файл
├── run-tests.ts       # Главный скрипт запуска всех тестов
└── test-coverage.ts   # Скрипт детального анализа coverage
```

## 🚀 Основные скрипты

### `run-tests.ts`

Главный скрипт для запуска всего набора тестов с coverage reporting.

#### Функциональность
- ✅ Запуск unit тестов с coverage
- ✅ Запуск integration тестов с coverage  
- ✅ Запуск E2E тестов (Playwright)
- ✅ Генерация coverage отчетов (HTML, LCOV, JSON)
- ✅ Автоматический запуск/остановка preview сервера
- ✅ Подробные отчеты о результатах

#### Использование

```bash
# Запуск всех тестов
deno run --allow-read --allow-write --allow-run --allow-net scripts/run-tests.ts

# Или через npm script
pnpm test:all
```

#### Особенности

- **Автоматическое управление окружением**: Запускает preview сервер для E2E тестов
- **Comprehensive reporting**: Создает HTML, LCOV и JSON отчеты
- **Error handling**: Graceful обработка ошибок с детальными сообщениями
- **Progress tracking**: Показывает прогресс и время выполнения
- **Multi-format output**: Результаты в различных форматах для разных нужд

#### Структура coverage отчетов

```
coverage/
├── html/                   # HTML отчет для просмотра в браузере
├── coverage-final.json    # JSON данные для анализа
├── coverage.lcov          # LCOV формат для CI/CD систем
└── playwright/            # E2E тест результаты
    ├── results.json       # Результаты Playwright
    └── videos/            # Видео проваленных тестов
```

### `test-coverage.ts`

Скрипт для детального анализа coverage данных и генерации расширенных отчетов.

#### Функциональность
- 📊 Генерация основных coverage отчетов
- 🔍 Детальный анализ по компонентам
- 📈 Анализ по типам тестов
- 📋 Создание dashboard с метриками
- 📝 Отчеты по файлам с низким покрытием
- 🎯 Рекомендации по улучшению coverage

#### Использование

```bash
# Полный анализ coverage
deno run --allow-read --allow-write --allow-net scripts/test-coverage.ts

# Или через npm script
pnpm test:analyze
```

#### Генерируемые отчеты

1. **Dashboard** (`detailed/dashboard.md`)
   - Общая статистика coverage
   - Статус по компонентам
   - Цели и отклонения
   - Список файлов с низким покрытием

2. **Component Analysis** (`detailed/component-analysis.md`)
   - Coverage по компонентам (Components, Hooks, Utils, Pages, Lib)
   - Статусы с цветовой индикацией
   - Рекомендации по улучшению

3. **Test Types Analysis** (`detailed/test-types-analysis.md`)
   - Анализ по типам тестов (Unit, Integration, E2E)
   - Статус каждого типа тестов
   - Coverage метрики

4. **Files Analysis** (`detailed/files-analysis.md`)
   - Детальный анализ покрытия по файлам
   - Сортировка по уровню покрытия
   - Статистика по файлам

5. **Functions Analysis** (`detailed/functions-analysis.md`)
   - Покрытие функций
   - Анализ uncovered функций
   - Приоритеты для улучшения

6. **Lines Analysis** (`detailed/lines-analysis.md`)
   - Анализ покрытия строк кода
   - Непокрытые строки
   - Критические области

#### Структура детальных отчетов

```
coverage/detailed/
├── dashboard.md           # Главный dashboard
├── component-analysis.md  # Анализ по компонентам
├── test-types-analysis.md # Анализ по типам тестов
├── files-analysis.md      # Анализ по файлам
├── functions-analysis.md  # Анализ функций
└── lines-analysis.md      # Анализ строк
```

## 🔧 Настройка и конфигурация

### Environment переменные

Скрипты поддерживают следующие переменные окружения:

```bash
# Port для preview сервера
export PORT=4173
export HOST=0.0.0.0

# Директории для coverage
export COVERAGE_DIR=.deno/coverage
export REPORT_DIR=coverage

# Timeout настройки
export TEST_TIMEOUT=30000
export E2E_TIMEOUT=60000

# Debug режим
export DEBUG=true
```

### Настройка Deno

Скрипты используют современные возможности Deno:

```bash
# Необходимые permissions
--allow-read      # Чтение файлов проекта
--allow-write     # Запись coverage отчетов
--allow-run       # Запуск внешних команд (pnpm, deno test)
--allow-net       # Сетевые запросы для preview сервера
```

### Интеграция с package.json

Скрипты интегрированы через npm scripts:

```json
{
  "scripts": {
    "test:all": "deno run --allow-read --allow-write --allow-run --allow-net scripts/run-tests.ts",
    "test:analyze": "deno run --allow-read --allow-write --allow-net scripts/test-coverage.ts",
    "test:coverage": "deno coverage merge --output=coverage/coverage-final.json .deno/coverage",
    "coverage:report": "deno coverage html --output-dir=coverage/html && deno coverage lcov --output=coverage/coverage.lcov coverage/coverage-final.json",
    "coverage:serve": "python3 -m http.server 8080 --directory coverage/html"
  }
}
```

## 📊 Coverage метрики

### Цели покрытия

| Компонент | Цель | Текущий статус |
|-----------|------|----------------|
| **Общий coverage** | 80% | Автоматический расчет |
| **Components** | 70% | По компонентам |
| **Hooks** | 85% | По hooks |
| **Utils** | 95% | По утилитам |
| **API Services** | 80% | По сервисам |

### Методы анализа

1. **Line Coverage** - процент выполненных строк кода
2. **Function Coverage** - процент вызванных функций
3. **Branch Coverage** - процент пройденных ветвлений
4. **Statement Coverage** - процент выполненных statements

## 🎯 Рекомендации по использованию

### Для разработки

1. **Ежедневная разработка**:
   ```bash
   pnpm test:unit          # Быстрая проверка unit тестов
   pnpm test:watch         # Watch режим во время разработки
   ```

2. **Перед commit**:
   ```bash
   pnpm test:ci            # Полная проверка как в CI
   ```

3. **Weekly coverage review**:
   ```bash
   pnpm test:analyze       # Детальный анализ coverage
   pnpm coverage:serve     # Просмотр HTML отчета
   ```

### Для CI/CD

1. **GitHub Actions**:
   - Автоматический запуск `pnpm test:all`
   - Coverage upload в Codecov
   - PR комментарии с метриками

2. **Coverage gates**:
   - Минимальный coverage: 80%
   - Fail если coverage снижается
   - Артефакты для скачивания

### Для ревью

1. **Coverage trending**:
   - Отслеживание изменений coverage
   - Identifications проблемных областей
   - Рекомендации по улучшению

2. **Quality gates**:
   - Проверка критических компонентов
   - Анализ regression в coverage
   - Автоматические уведомления

## 🚨 Troubleshooting

### Частые проблемы

**❌ Permission denied errors**
```bash
# Убедитесь что Deno имеет правильные permissions
deno run --allow-all scripts/run-tests.ts

# Или добавьте в deno.json
{
  "permissions": {
    "read": true,
    "write": true,
    "run": true,
    "net": true
  }
}
```

**❌ Coverage данные не генерируются**
```bash
# Проверьте что .deno/coverage директория существует
mkdir -p .deno/coverage

# Убедитесь что тесты запускаются с --coverage flag
deno test --coverage=.deno/coverage src/**/*.test.ts
```

**❌ E2E тесты не могут подключиться к серверу**
```bash
# Проверьте что порт свободен
lsof -i :4173

# Запустите preview сервер отдельно
pnpm preview --host 0.0.0.0 --port 4173
```

**❌ Скрипты работают медленно**
```bash
# Используйте --jobs флаг для параллельности
deno test --jobs=4 src/**/*.test.ts

# Очистите кэш coverage
rm -rf .deno/coverage/*
```

### Debug режим

```bash
# Включите debug логи
export DEBUG=true
pnpm test:all

# Детальная информация о coverage
deno run --allow-read --allow-write scripts/test-coverage.ts --verbose
```

## 🔗 Интеграции

### External Tools

1. **Codecov**: Автоматическая отправка coverage данных
2. **GitHub PR**: Комментарии с coverage в Pull Request
3. **SonarQube**: Интеграция для enterprise анализа
4. **Coveralls**: Альтернативная coverage платформа

### CI/CD Integration

```yaml
# GitHub Actions пример
- name: Run comprehensive tests
  run: |
    pnpm test:all
    pnpm test:analyze

- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v4
  with:
    file: ./coverage/coverage-final.json
```

### IDE Integration

1. **VSCode**: Интеграция с Coverage Gutters
2. **WebStorm**: Встроенная поддержка coverage
3. **Vim/Neovim**: Плагины для coverage highlighting

## 📈 Производительность

### Оптимизация

- **Параллельное выполнение**: Использование `--jobs` флага
- **Кэширование**: Deno cache для зависимостей
- **Избирательность**: Запуск только измененных тестов
- **Resource management**: Ограничение использования памяти

### Мониторинг

```bash
# Профилирование выполнения
deno run --profile scripts/run-tests.ts

# Memory usage
time deno run scripts/run-tests.ts

# Coverage size optimization
du -sh coverage/
```

---

## 💡 Лучшие практики

1. **Регулярный анализ**: Запускайте `pnpm test:analyze` еженедельно
2. **CI интеграция**: Настройте автоматические проверки в pipeline
3. **Coverage trends**: Отслеживайте изменения coverage во времени
4. **Team reviews**: Используйте coverage отчеты в код ревью
5. **Continuous improvement**: Постепенно повышайте цели coverage

**Цель**: Поддерживать coverage ≥ 80% с детальными отчетами для continuous quality improvement.
