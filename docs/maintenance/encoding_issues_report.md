# Отчет о проблемах с кодировкой в commit messages

**Дата проверки:** 2025-11-13  
**Проверено коммитов:** 30  
**Найдено проблем:** ~25 коммитов с некорректной кодировкой кириллицы

## Проблема

Многие commit messages содержат кириллицу, которая отображается как кракозябры (например, `СѓС‚Рѕ` вместо `уточнить`). Это происходит из-за неправильной обработки кодировки UTF-8 в Windows/PowerShell при создании коммитов.

## Примеры проблемных коммитов

- `1d328ff` - `ci: СЃРёРЅС…СЂРѕРЅРёР·РёСЂРѕРІР°С‚СЊ...` (должно быть: `ci: sync coverage threshold...`)
- `fd0f5bb` - `tests: СЃС‚Р°Р±РёР»РёР·РёСЂРѕРІР°С‚СЊ...` (должно быть: `tests: stabilize...`)
- `82f3db0` - `tests: РїРѕРґРіРѕС‚РѕРІРёС‚СЊ...` (должно быть: `tests: prepare...`)
- И еще ~22 коммита

## Решение

### ✅ Выполнено

1. **Настроен git для правильной работы с UTF-8:**
   ```bash
   git config --global i18n.commitencoding utf-8
   git config --global core.quotepath false
   ```

2. **Исправлен последний проблемный коммит** (`1d328ff` → `8b6c0a7`)

3. **Создан скрипт для проверки:** `scripts/maintenance/fix_commit_encoding.py`

### 📋 Рекомендации

#### Для будущих коммитов:

1. **Используйте английский язык для commit messages** (рекомендуется)
   - Избегает проблем с кодировкой
   - Универсально для международных команд
   - Соответствует best practices

2. **Если нужна кириллица**, убедитесь что:
   - Git настроен: `git config --global i18n.commitencoding utf-8`
   - PowerShell использует UTF-8: `$PSDefaultParameterValues['*:Encoding'] = 'utf8'`
   - Или используйте Git Bash вместо PowerShell

#### Для исправления старых коммитов:

Если нужно исправить старые коммиты (⚠️ **осторожно: переписывает историю**):

```bash
# Interactive rebase последних N коммитов
git rebase -i HEAD~N

# В редакторе измените 'pick' на 'reword' для проблемных коммитов
# Затем обновите сообщения на английском
```

**Внимание:** Не переписывайте историю для коммитов, которые уже были синхронизированы с другими разработчиками!

## Проверка файлов проекта

✅ **Файлы проекта в порядке** - все файлы используют правильную кодировку UTF-8:
- Markdown файлы: ✅ UTF-8
- Python скрипты: ✅ UTF-8  
- Конфигурационные файлы: ✅ UTF-8

Проблема была только в commit messages, не в содержимом файлов.

## Инструменты

- **Проверка:** `python scripts/maintenance/fix_commit_encoding.py`
- **Исправление:** Используйте `git commit --amend` для последнего коммита или `git rebase -i` для нескольких

## Статус

- ✅ Git настроен для UTF-8
- ✅ Будущие коммиты будут корректными
- ⚠️ Старые коммиты остаются с проблемами (можно исправить через rebase при необходимости)

