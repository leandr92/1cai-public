# Стандарты Документирования Проекта

## Обзор

Этот документ определяет стандарты документирования для проекта 1C AI. Все разработчики должны следовать этим стандартам при создании и обновлении документации.

---

## 1. Docstrings для Python

### 1.1 Стиль: Google Style

Мы используем **Google Style** для docstrings. Это читаемый и широко принятый формат.

### 1.2 Структура Docstring

#### Модули

```python
"""Краткое описание модуля (одна строка).

Более подробное описание модуля, если необходимо.
Может занимать несколько строк.

Пример использования:
    from module import function
    result = function()

Attributes:
    module_level_variable (str): Описание переменной уровня модуля.
"""
```

#### Классы

```python
class ExampleClass:
    """Краткое описание класса.
    
    Более подробное описание класса и его назначения.
    
    Attributes:
        attribute1 (str): Описание атрибута 1.
        attribute2 (int): Описание атрибута 2.
        
    Example:
        >>> obj = ExampleClass("test", 42)
        >>> obj.method()
        'result'
    """
    
    def __init__(self, param1: str, param2: int):
        """Инициализация экземпляра класса.
        
        Args:
            param1: Описание параметра 1.
            param2: Описание параметра 2.
        """
        self.attribute1 = param1
        self.attribute2 = param2
```

#### Функции

```python
def example_function(param1: str, param2: int = 0) -> bool:
    """Краткое описание функции.
    
    Более подробное описание того, что делает функция.
    Может включать детали реализации, если это важно.
    
    Args:
        param1: Описание первого параметра.
        param2: Описание второго параметра. По умолчанию 0.
        
    Returns:
        True если успешно, False иначе.
        
    Raises:
        ValueError: Если param1 пустая строка.
        TypeError: Если param2 не число.
        
    Example:
        >>> example_function("test", 42)
        True
        >>> example_function("", 0)
        ValueError: param1 не может быть пустым
    """
    if not param1:
        raise ValueError("param1 не может быть пустым")
    return True
```

### 1.3 Обязательные Элементы

| Тип | Краткое описание | Args | Returns | Raises | Example |
|-----|------------------|------|---------|--------|---------|
| Модуль | ✅ Обязательно | ❌ | ❌ | ❌ | ⭐ Желательно |
| Класс | ✅ Обязательно | ❌ | ❌ | ❌ | ⭐ Желательно |
| Публичная функция | ✅ Обязательно | ✅ Обязательно | ✅ Обязательно | ⭐ Если есть | ⭐ Желательно |
| Приватная функция | ⭐ Желательно | ⭐ Желательно | ⭐ Желательно | ❌ | ❌ |

### 1.4 Язык Docstrings

- **Основной язык:** Русский
- **Английский:** Только для технических терминов, которые не имеют устоявшегося русского перевода
- **Примеры:** Всегда на русском с комментариями

**Хорошо:**
```python
def calculate_metrics(data: List[float]) -> Dict[str, float]:
    """Вычисляет статистические метрики для данных.
    
    Функция вычисляет среднее значение, медиану и стандартное
    отклонение для переданного списка чисел.
    
    Args:
        data: Список числовых значений для анализа.
        
    Returns:
        Словарь с ключами 'mean', 'median', 'std'.
    """
```

**Плохо:**
```python
def calculate_metrics(data):
    """Calculate metrics"""  # Слишком кратко, на английском
```

---

## 2. Markdown Документация

### 2.1 Структура Файла

```markdown
# Заголовок Документа

## Обзор

Краткое описание того, о чём этот документ.

---

## Раздел 1

Содержание раздела.

### Подраздел 1.1

Детали подраздела.

---

## Примеры

\`\`\`python
# Пример кода с комментариями на русском
result = function()
\`\`\`

---

## См. также

- [Связанный документ 1](./related1.md)
- [Связанный документ 2](./related2.md)
```

### 2.2 Правила Форматирования

1. **Заголовки:**
   - H1 (`#`) - только один на документ (название)
   - H2 (`##`) - основные разделы
   - H3 (`###`) - подразделы
   - Не использовать H4+ без крайней необходимости

2. **Списки:**
   - Ненумерованные: `-` (дефис с пробелом)
   - Нумерованные: `1.`, `2.`, etc.
   - Вложенные: 2 пробела отступ

3. **Код:**
   - Inline: \`код\`
   - Блоки: \`\`\`язык ... \`\`\`
   - Всегда указывать язык для подсветки

4. **Ссылки:**
   - Внутренние: относительные пути `[текст](./path/to/file.md)`
   - Внешние: полные URL `[текст](https://example.com)`
   - Якоря: `[текст](#section-name)`

5. **Таблицы:**
   ```markdown
   | Колонка 1 | Колонка 2 |
   |-----------|-----------|
   | Значение 1 | Значение 2 |
   ```

### 2.3 Именование Файлов

- **Формат:** `НАЗВАНИЕ_В_ВЕРХНЕМ_РЕГИСТРЕ.md` для главных документов
- **Формат:** `название-в-нижнем-регистре.md` для обычных документов
- **Примеры:**
  - `README.md` - главный файл проекта
  - `CONTRIBUTING.md` - руководство для контрибьюторов
  - `api-reference.md` - справочник API
  - `quick-start.md` - быстрый старт

---

## 3. Структура Документации

### 3.1 Корневая Директория

```
/
├── README.md                 # Главная страница проекта
├── CHANGELOG.md             # История изменений
├── CONTRIBUTING.md          # Руководство для разработчиков
├── LICENSE                  # Лицензия
└── docs/                    # Вся документация
    ├── README.md            # Индекс документации
    ├── 01-getting-started/  # Начало работы
    ├── 02-architecture/     # Архитектура
    ├── 03-ai-agents/        # AI агенты
    ├── 04-deployment/       # Развёртывание
    ├── 05-development/      # Разработка
    ├── 06-features/         # Функциональность
    └── api/                 # API документация
```

### 3.2 Обязательные Документы

1. **README.md** (корень)
   - Описание проекта
   - Быстрый старт
   - Основные возможности
   - Ссылки на документацию

2. **docs/README.md**
   - Индекс всей документации
   - Карта документации
   - Навигация по разделам

3. **CONTRIBUTING.md**
   - Как внести вклад
   - Стандарты кода
   - Процесс review
   - Контакты

4. **CHANGELOG.md**
   - История версий
   - Формат: Keep a Changelog

---

## 4. Примеры Хорошей Документации

### 4.1 Модуль с AI Функциональностью

```python
"""Модуль для работы с AI моделями и провайдерами.

Этот модуль предоставляет унифицированный интерфейс для работы
с различными AI провайдерами (OpenAI, Anthropic, Gemini, etc.).

Основные компоненты:
    - LLMProvider: Базовый класс для провайдеров
    - OpenAIProvider: Реализация для OpenAI
    - AnthropicProvider: Реализация для Anthropic

Пример использования:
    from ai.providers import OpenAIProvider
    
    provider = OpenAIProvider(api_key="your-key")
    response = await provider.generate("Привет, как дела?")
    print(response.text)

Attributes:
    DEFAULT_TIMEOUT (int): Таймаут по умолчанию для запросов (30 сек).
    MAX_RETRIES (int): Максимальное количество повторных попыток (3).
"""

from typing import Optional, Dict, Any
import asyncio


class LLMProvider:
    """Базовый класс для AI провайдеров.
    
    Определяет общий интерфейс для всех провайдеров LLM.
    Наследники должны реализовать метод generate().
    
    Attributes:
        api_key: API ключ для провайдера.
        model: Название модели для использования.
        timeout: Таймаут для запросов в секундах.
    """
    
    def __init__(
        self,
        api_key: str,
        model: str = "default",
        timeout: int = 30
    ):
        """Инициализация провайдера.
        
        Args:
            api_key: API ключ для аутентификации.
            model: Название модели. По умолчанию "default".
            timeout: Таймаут запросов в секундах. По умолчанию 30.
            
        Raises:
            ValueError: Если api_key пустой.
        """
        if not api_key:
            raise ValueError("API ключ не может быть пустым")
            
        self.api_key = api_key
        self.model = model
        self.timeout = timeout
    
    async def generate(
        self,
        prompt: str,
        **kwargs: Any
    ) -> Dict[str, Any]:
        """Генерирует ответ на основе промпта.
        
        Этот метод должен быть переопределён в наследниках.
        
        Args:
            prompt: Текст промпта для модели.
            **kwargs: Дополнительные параметры для модели.
            
        Returns:
            Словарь с ключами:
                - text: Сгенерированный текст
                - tokens: Количество использованных токенов
                - model: Использованная модель
                
        Raises:
            NotImplementedError: Если метод не переопределён.
            
        Example:
            >>> provider = OpenAIProvider(api_key="key")
            >>> result = await provider.generate("Привет!")
            >>> print(result['text'])
            'Привет! Как я могу помочь?'
        """
        raise NotImplementedError(
            "Метод generate() должен быть реализован в наследнике"
        )
```

### 4.2 README для Модуля

```markdown
# AI Провайдеры

## Обзор

Модуль для работы с различными AI провайдерами через единый интерфейс.

## Поддерживаемые Провайдеры

- ✅ OpenAI (GPT-4, GPT-3.5)
- ✅ Anthropic (Claude)
- ✅ Google (Gemini)
- ✅ Yandex (YandexGPT)
- 🔄 Ollama (в разработке)

## Быстрый Старт

\`\`\`python
from ai.providers import OpenAIProvider

# Создание провайдера
provider = OpenAIProvider(
    api_key="your-api-key",
    model="gpt-4"
)

# Генерация ответа
response = await provider.generate("Привет, мир!")
print(response['text'])
\`\`\`

## Конфигурация

См. [Руководство по конфигурации](./configuration.md)

## API Документация

См. [API Reference](../../api/providers.md)
```

---

## 5. Инструменты и Автоматизация

### 5.1 Pre-commit Hooks

Установите pre-commit hooks для автоматической проверки:

```bash
# .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: check-docstrings
        name: Check Python docstrings
        entry: python scripts/quality/check_docstring_coverage.py
        language: system
        types: [python]
```

### 5.2 CI/CD Проверки

GitHub Actions автоматически проверяют:
- Наличие docstrings
- Broken links в markdown
- Форматирование markdown

### 5.3 VS Code Snippets

Используйте snippets для быстрого создания docstrings:

```json
{
  "Google Style Docstring": {
    "prefix": "docstring",
    "body": [
      "\"\"\"${1:Краткое описание}.",
      "",
      "${2:Подробное описание}.",
      "",
      "Args:",
      "    ${3:param}: ${4:Описание параметра}.",
      "",
      "Returns:",
      "    ${5:Описание возвращаемого значения}.",
      "\"\"\"",
      "$0"
    ]
  }
}
```

---

## 6. Чеклист для Review

При review документации проверяйте:

- [ ] Все публичные функции имеют docstrings
- [ ] Docstrings на русском языке
- [ ] Все параметры описаны в Args
- [ ] Возвращаемое значение описано в Returns
- [ ] Исключения описаны в Raises (если есть)
- [ ] Есть примеры использования (для сложных функций)
- [ ] Нет broken links
- [ ] Markdown правильно отформатирован
- [ ] Код в примерах работает

---

## 7. Контакты и Поддержка

Вопросы по документированию:
- GitHub Issues: [создать issue](https://github.com/your-repo/issues)
- Документация: [docs/](../README.md)

---

**Версия:** 1.0  
**Дата обновления:** 2025-11-28  
**Авторы:** Команда разработки 1C AI
