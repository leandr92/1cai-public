# 🤖 ML Dataset Generator

> **Автоматическая генерация обучающих dataset для fine-tuning моделей на коде 1С**

---

## 🎯 Что это такое?

**ML Dataset Generator** — это инструмент для создания высококачественных обучающих датасетов из исходного кода 1С:Enterprise для fine-tuning языковых моделей.

### Возможности:

- 📊 **Автоматическая категоризация** - 7 типов задач
- 🔍 **Обогащение контекстом** - метаданные, зависимости, паттерны
- 📈 **Качественная фильтрация** - только валидный и полезный код
- 💾 **Множество форматов** - JSON, JSONL, CSV, Parquet
- 🎓 **Готов к обучению** - совместим с OpenAI, HuggingFace, LoRA

### Статистика:

```
Создано примеров:        24,000+
Категорий кода:          7
Средний размер:          ~250 токенов
Качество фильтрации:     95%+
```

---

## 🚀 Quick Start

### 1. Подготовка данных

Сначала нужно спарсить конфигурацию EDT:

```bash
# Парсинг конфигурации
python scripts/parsers/edt/edt_parser_with_metadata.py \
    --source /path/to/configuration \
    --output output/edt_parser/ \
    --include-code \
    --extract-dependencies
```

**Результат:** `output/edt_parser/full_parse_with_metadata.json`

### 2. Генерация dataset

```bash
# Создание ML dataset
python scripts/dataset/create_ml_dataset.py \
    --input output/edt_parser/full_parse_with_metadata.json \
    --output output/ml_dataset/
```

### 3. Результат

```
output/ml_dataset/
├── training_dataset.jsonl        # Основной dataset (JSONL)
├── training_dataset.json         # JSON формат
├── dataset_statistics.json       # Статистика dataset
└── category_examples.json        # Примеры по категориям
```

---

## 📊 Категории кода (7 типов)

Dataset автоматически категоризирует код на 7 типов задач:

### 1. **Запросы к данным (Query)**

Функции работы с запросами, выборками, агрегацией:

```bsl
Функция ПолучитьОстаткиНоменклатуры(Дата, Склад)
    Запрос = Новый Запрос;
    Запрос.Текст = "
        |ВЫБРАТЬ
        |    Номенклатура,
        |    СУМ(Количество) КАК Остаток
        |ИЗ РегистрНакопления.ОстаткиТоваров.Остатки(&Дата, Склад = &Склад)
        |СГРУППИРОВАТЬ ПО Номенклатура";
    Запрос.УстановитьПараметр("Дата", Дата);
    Запрос.УстановитьПараметр("Склад", Склад);
    Возврат Запрос.Выполнить().Выгрузить();
КонецФункции
```

### 2. **Обработка документов (Document Processing)**

Работа с документами - проведение, заполнение, проверки:

```bsl
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
    // Проверка заполнения
    Если НЕ ЗначениеЗаполнено(Контрагент) Тогда
        Отказ = Истина;
        Сообщить("Не указан контрагент");
        Возврат;
    КонецЕсли;
    
    // Движения по регистрам
    Движения.ВзаиморасчетыСКонтрагентами.Записывать = Истина;
    Для Каждого СтрокаТЧ Из Товары Цикл
        Движение = Движения.ВзаиморасчетыСКонтрагентами.Добавить();
        Движение.Контрагент = Контрагент;
        Движение.Сумма = СтрокаТЧ.Сумма;
    КонецЦикла;
КонецПроцедуры
```

### 3. **Валидация и проверки (Validation)**

Функции проверки данных, валидации:

```bsl
Функция ПроверитьИНН(ИНН) Экспорт
    Если СтрДлина(ИНН) <> 10 И СтрДлина(ИНН) <> 12 Тогда
        Возврат Ложь;
    КонецЕсли;
    
    // Проверка контрольных сумм
    Если СтрДлина(ИНН) = 10 Тогда
        Возврат ПроверитьКонтрольнуюСуммуИНН10(ИНН);
    Иначе
        Возврат ПроверитьКонтрольнуюСуммуИНН12(ИНН);
    КонецЕсли;
КонецФункции
```

### 4. **Работа со справочниками (Reference Processing)**

CRUD операции со справочниками:

```bsl
Функция СоздатьНоменклатуру(Наименование, Артикул, ЕдиницаИзмерения)
    НоменклатураОбъект = Справочники.Номенклатура.СоздатьЭлемент();
    НоменклатураОбъект.Наименование = Наименование;
    НоменклатураОбъект.Артикул = Артикул;
    НоменклатураОбъект.БазоваяЕдиницаИзмерения = ЕдиницаИзмерения;
    
    Попытка
        НоменклатураОбъект.Записать();
        Возврат НоменклатураОбъект.Ссылка;
    Исключение
        Сообщить(ОписаниеОшибки());
        Возврат Неопределено;
    КонецПопытки;
КонецФункции
```

### 5. **Трансформация данных (Data Transformation)**

Преобразование, агрегация, группировка:

```bsl
Функция ПреобразоватьТаблицуВДерево(ТаблицаЗначений)
    ДеревоЗначений = Новый ДеревоЗначений;
    ДеревоЗначений.Колонки.Добавить("Наименование");
    ДеревоЗначений.Колонки.Добавить("Значение");
    
    СтруктураГруппировки = Новый Соответствие;
    
    Для Каждого Строка Из ТаблицаЗначений Цикл
        Родитель = СтруктураГруппировки[Строка.Группа];
        Если Родитель = Неопределено Тогда
            Родитель = ДеревоЗначений.Строки.Добавить();
            Родитель.Наименование = Строка.Группа;
            СтруктураГруппировки.Вставить(Строка.Группа, Родитель);
        КонецЕсли;
        
        ДочернийЭлемент = Родитель.Строки.Добавить();
        ДочернийЭлемент.Наименование = Строка.Наименование;
        ДочернийЭлемент.Значение = Строка.Значение;
    КонецЦикла;
    
    Возврат ДеревоЗначений;
КонецФункции
```

### 6. **Расчеты и алгоритмы (Calculation)**

Математические расчеты, бизнес-логика:

```bsl
Функция РассчитатьНДС(Сумма, СтавкаНДС)
    Если СтавкаНДС = Перечисления.СтавкиНДС.НДС20 Тогда
        Возврат Окр(Сумма * 0.20, 2);
    ИначеЕсли СтавкаНДС = Перечисления.СтавкиНДС.НДС10 Тогда
        Возврат Окр(Сумма * 0.10, 2);
    Иначе
        Возврат 0;
    КонецЕсли;
КонецФункции

Функция РассчитатьСкидку(Сумма, ПроцентСкидки)
    СуммаСкидки = Окр(Сумма * ПроцентСкидки / 100, 2);
    Возврат Сумма - СуммаСкидки;
КонецФункции
```

### 7. **Работа с формами (Form Processing)**

Обработчики событий форм, управление интерфейсом:

```bsl
&НаКлиенте
Процедура ПриОткрытии(Отказ)
    ОбновитьСписокНоменклатуры();
    УстановитьДоступностьЭлементов();
КонецПроцедуры

&НаСервере
Процедура УстановитьДоступностьЭлементов()
    Элементы.ТоварыКоличество.ТолькоПросмотр = НЕ РазрешеноРедактирование;
    Элементы.ТоварыЦена.Видимость = ПоказыватьЦены;
КонецПроцедуры
```

---

## 📖 Использование

### Базовая генерация

```bash
python scripts/dataset/create_ml_dataset.py \
    --input output/edt_parser/full_parse_with_metadata.json \
    --output output/ml_dataset/
```

### С фильтрацией

```bash
# Только определенные категории
python scripts/dataset/create_ml_dataset.py \
    --input output/edt_parser/full_parse_with_metadata.json \
    --output output/ml_dataset/ \
    --categories Query,Calculation,Validation

# С фильтром по размеру
python scripts/dataset/create_ml_dataset.py \
    --input output/edt_parser/full_parse_with_metadata.json \
    --output output/ml_dataset/ \
    --min-tokens 50 \
    --max-tokens 500

# С фильтром по качеству
python scripts/dataset/create_ml_dataset.py \
    --input output/edt_parser/full_parse_with_metadata.json \
    --output output/ml_dataset/ \
    --min-quality-score 0.8
```

### Python API

```python
from scripts.dataset.create_ml_dataset import MLDatasetGenerator

# Создание генератора
generator = MLDatasetGenerator(
    input_path="output/edt_parser/full_parse_with_metadata.json",
    output_path="output/ml_dataset/",
    options={
        'categories': ['Query', 'Document Processing', 'Validation'],
        'min_tokens': 50,
        'max_tokens': 500,
        'min_quality_score': 0.8,
        'include_context': True,
        'deduplicate': True
    }
)

# Генерация dataset
stats = generator.generate()

print(f"Создано примеров: {stats['total_examples']}")
print(f"По категориям: {stats['by_category']}")
```

---

## 📊 Формат dataset

### JSONL (для обучения)

```jsonl
{"prompt": "Напиши функцию для получения остатков товаров на складе", "completion": "Функция ПолучитьОстатки(Склад)\n    Запрос = Новый Запрос;\n    ...\nКонецФункции", "category": "Query", "metadata": {"complexity": 5, "lines": 15}}
{"prompt": "Создай процедуру проведения документа реализации", "completion": "Процедура ОбработкаПроведения(Отказ)\n    ...\nКонецПроцедуры", "category": "Document Processing", "metadata": {"complexity": 8, "lines": 45}}
```

### JSON (детальный формат)

```json
{
  "examples": [
    {
      "id": "example_001",
      "category": "Query",
      "prompt": "Напиши функцию для получения остатков товаров на складе по состоянию на дату",
      "completion": "Функция ПолучитьОстаткиНоменклатуры(Дата, Склад)\n    Запрос = Новый Запрос;\n    Запрос.Текст = \"\n        |ВЫБРАТЬ\n        |    Номенклатура,\n        |    СУМ(Количество) КАК Остаток\n        |ИЗ РегистрНакопления.ОстаткиТоваров.Остатки(&Дата, Склад = &Склад)\n        |СГРУППИРОВАТЬ ПО Номенклатура\";\n    Запрос.УстановитьПараметр(\"Дата\", Дата);\n    Запрос.УстановитьПараметр(\"Склад\", Склад);\n    Возврат Запрос.Выполнить().Выгрузить();\nКонецФункции",
      "metadata": {
        "source_object": "CommonModule.РаботаСНоменклатурой",
        "function_name": "ПолучитьОстаткиНоменклатуры",
        "complexity": 5,
        "lines_of_code": 12,
        "api_calls": ["Запрос", "РегистрНакопления"],
        "parameters": ["Дата", "Склад"],
        "return_type": "ТаблицаЗначений"
      },
      "context": {
        "object_type": "CommonModule",
        "module_purpose": "Работа с номенклатурой и остатками",
        "related_objects": ["Справочники.Номенклатура", "РегистрНакопления.ОстаткиТоваров"]
      },
      "quality_score": 0.95,
      "tokens": 247
    }
  ],
  "metadata": {
    "total_examples": 24156,
    "generation_date": "2025-11-06T23:59:59",
    "source_configuration": "УправлениеПредприятием v3.0",
    "categories": {
      "Query": 4820,
      "Document Processing": 5240,
      "Validation": 3100,
      "Reference Processing": 2890,
      "Data Transformation": 3640,
      "Calculation": 2466,
      "Form Processing": 2000
    }
  }
}
```

---

## 🎓 Обучение моделей

### OpenAI Fine-tuning

```bash
# 1. Конвертация в OpenAI формат
python scripts/dataset/convert_to_openai.py \
    --input output/ml_dataset/training_dataset.jsonl \
    --output output/ml_dataset/openai_format.jsonl

# 2. Загрузка в OpenAI
openai api fine_tunes.create \
    -t output/ml_dataset/openai_format.jsonl \
    -m davinci \
    --suffix "1c-bsl-code"
```

### HuggingFace

```python
from datasets import load_dataset
from transformers import AutoTokenizer, AutoModelForCausalLM, Trainer, TrainingArguments

# Загрузка dataset
dataset = load_dataset('json', data_files='output/ml_dataset/training_dataset.jsonl')

# Модель и токенизатор
model_name = "microsoft/CodeGPT-small-py"
tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForCausalLM.from_pretrained(model_name)

# Токенизация
def tokenize_function(examples):
    return tokenizer(
        examples["prompt"] + " " + examples["completion"],
        truncation=True,
        max_length=512
    )

tokenized_dataset = dataset.map(tokenize_function, batched=True)

# Обучение
training_args = TrainingArguments(
    output_dir="./models/1c-bsl-finetuned",
    num_train_epochs=3,
    per_device_train_batch_size=4,
    save_steps=1000,
    save_total_limit=2,
)

trainer = Trainer(
    model=model,
    args=training_args,
    train_dataset=tokenized_dataset["train"],
)

trainer.train()
```

### LoRA Fine-tuning

```python
from peft import LoraConfig, get_peft_model, prepare_model_for_kbit_training
from transformers import AutoModelForCausalLM, AutoTokenizer, TrainingArguments
from trl import SFTTrainer

# Модель
model_name = "Qwen/Qwen2.5-Coder-7B"
model = AutoModelForCausalLM.from_pretrained(
    model_name,
    load_in_8bit=True,
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained(model_name)

# Подготовка к обучению
model = prepare_model_for_kbit_training(model)

# LoRA конфигурация
lora_config = LoraConfig(
    r=16,
    lora_alpha=32,
    target_modules=["q_proj", "v_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM"
)

model = get_peft_model(model, lora_config)

# Загрузка dataset
dataset = load_dataset('json', data_files='output/ml_dataset/training_dataset.jsonl')

# Тренировка
trainer = SFTTrainer(
    model=model,
    train_dataset=dataset["train"],
    dataset_text_field="completion",
    max_seq_length=512,
    tokenizer=tokenizer,
    args=TrainingArguments(
        output_dir="./models/1c-lora",
        num_train_epochs=3,
        per_device_train_batch_size=4,
        gradient_accumulation_steps=4,
        learning_rate=2e-4,
        fp16=True,
        save_total_limit=2,
        logging_steps=10,
    ),
)

trainer.train()
```

---

## 🔧 Опции генератора

### Командная строка

```bash
python scripts/dataset/create_ml_dataset.py \
    --input PATH                      # Входной JSON от EDT парсера
    --output PATH                     # Директория для сохранения
    --categories CAT1,CAT2            # Фильтр по категориям
    --min-tokens N                    # Минимум токенов (по умолчанию: 50)
    --max-tokens N                    # Максимум токенов (по умолчанию: 1000)
    --min-quality-score F             # Минимальный quality score (0-1)
    --include-context                 # Включить контекст объекта
    --deduplicate                     # Удалить дубликаты
    --split-ratio TRAIN:VAL:TEST      # Разделение на train/val/test
    --format FORMAT                   # json, jsonl, csv, parquet
    --verbose                         # Подробный вывод
```

### Python API

```python
generator = MLDatasetGenerator(
    input_path="output/edt_parser/full_parse_with_metadata.json",
    output_path="output/ml_dataset/",
    options={
        # Фильтры
        'categories': ['Query', 'Calculation'],
        'min_tokens': 50,
        'max_tokens': 500,
        'min_quality_score': 0.8,
        'min_complexity': 3,
        'max_complexity': 15,
        
        # Обогащение
        'include_context': True,
        'include_metadata': True,
        'include_dependencies': True,
        'add_comments': True,
        
        # Качество
        'deduplicate': True,
        'remove_test_code': True,
        'remove_empty_functions': True,
        'validate_syntax': True,
        
        # Аугментация
        'augment_data': True,
        'augmentation_factor': 2,
        
        # Разделение
        'split_ratio': (0.8, 0.1, 0.1),  # train, val, test
        
        # Формат
        'output_format': 'jsonl',
        'encoding': 'utf-8',
        
        # Прочее
        'verbose': True,
        'random_seed': 42
    }
)
```

---

## 📈 Качество dataset

### Quality Score

Каждый пример получает оценку качества (0-1) на основе:

1. **Полнота кода** (0-0.3)
   - Наличие всех необходимых элементов
   - Корректное завершение блоков

2. **Читаемость** (0-0.2)
   - Наличие комментариев
   - Понятные названия переменных

3. **Сложность** (0-0.2)
   - Оптимальная сложность (не слишком простой, не слишком сложный)
   - Cyclomatic complexity 3-15

4. **Использование API** (0-0.15)
   - Использование стандартных API 1С
   - Правильные паттерны

5. **Контекст** (0-0.15)
   - Наличие связанных объектов
   - Зависимости

### Фильтрация

Автоматически удаляются:
- ❌ Пустые функции
- ❌ Тестовый код (Test*, *Demo)
- ❌ Сгенерированный код (автоматические формы)
- ❌ Дубликаты
- ❌ Код с синтаксическими ошибками
- ❌ Слишком короткий код (<20 токенов)
- ❌ Слишком длинный код (>2000 токенов)

---

## 🎨 Аугментация данных

Опционально можно увеличить dataset через аугментацию:

```python
generator = MLDatasetGenerator(
    input_path="input.json",
    output_path="output/",
    options={
        'augment_data': True,
        'augmentation_factor': 2,  # Увеличить в 2 раза
        'augmentation_methods': [
            'rename_variables',      # Переименование переменных
            'reorder_parameters',    # Перестановка параметров
            'add_comments',          # Добавление комментариев
            'simplify_conditions',   # Упрощение условий
        ]
    }
)
```

---

## 🐛 Troubleshooting

### Проблема: Мало примеров в dataset

**Решение:**

```bash
# Уменьшите фильтры
python create_ml_dataset.py \
    --input input.json \
    --output output/ \
    --min-tokens 30 \          # Было 50
    --min-quality-score 0.6    # Было 0.8

# Или включите аугментацию
python create_ml_dataset.py \
    --input input.json \
    --output output/ \
    --augment-data \
    --augmentation-factor 3
```

### Проблема: Несбалансированные категории

**Решение:**

```python
# Балансировка категорий
generator = MLDatasetGenerator(
    options={
        'balance_categories': True,
        'samples_per_category': 3000
    }
)
```

---

## 📚 Примеры использования

### Создание специализированного dataset

```python
# Dataset только для query оптимизации
generator = MLDatasetGenerator(
    input_path="input.json",
    output_path="output/query_optimization/",
    options={
        'categories': ['Query'],
        'min_complexity': 5,
        'include_dependencies': True,
        'custom_prompts': [
            "Оптимизируй этот запрос:",
            "Как улучшить производительность:",
            "Перепиши запрос эффективнее:"
        ]
    }
)
```

---

## 📖 Связанные документы

- [EDT Parser Guide](EDT_PARSER_GUIDE.md) - парсинг конфигураций
- [BSL Fine-tuning Guide](BSL_FINETUNING_GUIDE.md) - обучение моделей
- [Architecture Overview](../02-architecture/ARCHITECTURE_OVERVIEW.md) - архитектура

---

*Последнее обновление: 6 ноября 2025*

