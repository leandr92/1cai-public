# 🛡️ Режим офлайн-устойчивости DevOps/SRE агента

## Цель
Обеспечить работоспособность агента при частичной или полной потере доступа к внешним LLM, не нарушая политики безопасности и регуляторные ограничения.

## 1. Когда включать режим
- Наблюдаются ошибки подключения к OpenAI/Gigachat/Yandex GPT (HTTP ≥ 500, тайм-аут > 40 c).
- Alert `ai_provider_status{provider="openai"} == "down"` от Prometheus/Alertmanager.
- Получено внутреннее уведомление о выключении доступа к глобальному интернету (ручной интернет, режим «белого списка»).
- Выполняется плановое учение «Интернет недоступен».

## 2. Ответственные роли
- **Drafter (on duty) DevOps** — инициирует переключение, отслеживает состояние.
- **SRE on-call** — контролирует стабильность внутренних сервисов, собирает метрики.
- **ML инженер** — проверяет качество ответов локальных моделей и корректирует параметры.
- **Security officer** — верифицирует отсутствие утечек и запрет на использование запрещённых API.

## 3. Подготовительные действия
1. Проверить доступность репозитория и текущих конфигураций `config/llm_providers.yaml`.
2. Убедиться, что secrets для fallback-провайдеров (Gigachat, Yandex, Naparnik) актуальны в Vault/K8s.
3. Прогреть векторную базу (`make build-vector-store`) не реже 1 раза в неделю.
4. Валидировать рабочее состояние `llm-gateway` (если развёрнут) командой `curl http://llm-gateway:8080/health`.

## 4. Процедура перехода в офлайн
1. **Диагностика**
   - Выполнить `python scripts/diagnostics/check_llm_endpoints.py --provider openai gigachat` и зафиксировать результат.
   - При подтверждении недоступности внешних API задокументировать в журнале инцидентов.
2. **Переключение**
   - Запустить `make switch-llm BACKEND=local-qwen` или указать нужного провайдера из `config/llm_providers.yaml`.
   - В случае отсутствия локальных моделей — активировать последовательность fallback-провайдеров (`gigachat → yandex-gpt → naparnik`) через команду `make switch-llm BACKEND=gigachat`.
3. **Обновление статусов**
   - Установить флаг `offline_mode=true` в конфигурации приложения (`src/config.py` → planned).
   - Отправить уведомление в канал #ai-assistant-status со ссылкой на журнал.
4. **Smoke-тесты**
   - Выполнить `pytest tests/integration/test_llm_failover.py -k smoke --maxfail=1`.
   - Зафиксировать результаты в runbook.

## 5. Работа агента в офлайн-режиме
- **Доступные функции**
  - Анализ конфигураций, запуск локальных скриптов, генерация рекомендаций по runbook.
  - Использование кэша знаний (векторная база из внутренних документов и `the-book-of-secret-knowledge`).
  - Предложения по автоматизированным действиям (Ansible, Terraform) — без генерации нового кода.
- **Ограниченные функции**
  - Свободная генерация длинных текстов/кодовых блоков (пока не интегрированы локальные модели).
  - Переводы и сложная reasoning-задачи, требующие GPT-4 уровня.
- **Коммуникация**
  - Агент информирует пользователя: «Режим ограниченной функциональности. Ответы основаны на локальной базе и fallback-моделях.»

## 6. Восстановление штатного режима
1. Мониторить статусы провайдеров; после двух успешных health-check включить `make switch-llm BACKEND=openai`.
2. Запустить A/B сравнение качества (`scripts/tests/compare_responses.py --baseline openai --candidate local-qwen`).
3. Обновить журнал инцидента, описать длительность и последствия.
4. Провести ретроспективу: какие шаги прошли успешно, что улучшить к следующему учению.

## 7. Плановые учения
- **Периодичность**: каждые 6 недель.
- **Сценарии**:
  1. Полный разрыв доступа к глобальному интернету (shutdown API).
  2. Медленная деградация (увеличение latency до 40 c, ошибки 429).
- **Метрики успеха**:
  - Время реакции до переключения < 15 минут.
  - Количество неуспешных запросов пользователей после переключения < 5%.
  - Качество ответов локальных моделей ≥ 80% от baseline (по smoke-набору).

## 8. Журнал и отчётность
- Используйте шаблон `docs/templates/offline_incident_report.md`.
- Храните отчёты в `analysis/offline-exercises/`.
- В отчётах фиксируйте:
  - время начала/окончания,
  - задействованный fallback,
  - проблемы и рекомендации.

## 9. Дополнительные ресурсы
- `analysis/llm_blocking_resilience_plan.md` — стратегический план.
- `config/llm_providers.yaml` — актуальные конфигурации провайдеров.
- `scripts/knowledge/build_vector_store.py` — сбор данных в локальную базу знаний.
- Источник рисков: статья «Ручной интернет» [1](https://www.svoboda.org/a/ruchnoy-internet/33586177.html).

---

Документ предназначен для внутреннего использования. Публикация вне компании запрещена без дополнительного согласования.

