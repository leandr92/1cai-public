# 🧪 QA Engineer AI Guide

**Статус:** ✅ Production  
**Файл:** `src/ai/agents/qa_engineer_agent_extended.py`

---

## 🎯 Назначение

QA Engineer AI помогает командам 1С автоматизировать тестовую стратегию:

- Генерация unit‑тестов для BSL/общих модулей
- Шаблоны Vanessa BDD и негативных сценариев
- Оценка покрытия и список edge‑cases
- Быстрый аудит требований/тестов на соответствие

Используется в BA/DevOps пайплайнах через Role-Based Router и MCP.

---

## 🧠 Основные возможности

1. **SmartTestGenerator**
   - Шаблоны `unit_test_bsl`, `vanessa_bdd`, `negative_test`
   - Автоматическое извлечение параметров и сложности функции
   - Расчёт условного покрытия (`coverage_estimate`)

2. **Edge Cases & Negative Paths**
   - Генерация наборов граничных значений
   - Проверка обработки исключений и сообщений об ошибках

3. **Integration Hooks**
   - Использование локальных моделей (Qwen 3 Coder) или внешних LLM
   - Выдача структуры, пригодной для загрузки в YAxUnit или Vanessa Automation

4. **Reports & Metadata**
   - JSON-ответ с блоками `tests`, `test_cases`, `vanessa_bdd`, `negative_tests`
   - Поля `complexity`, `parameters`, `recommendations`

---

## 🚀 Быстрый старт

```python
from src.ai.agents.qa_engineer_agent_extended import QAEngineerAgentExtended

agent = QAEngineerAgentExtended()
code = """
Функция РассчитатьСумму(Цена, Количество)
    Возврат Цена * Количество;
КонецФункции
"""

result = await agent.generate_tests(code, "РассчитатьСумму")
print(result["tests"]["unit_tests"][0])
```

CLI / MCP: используйте `make qa-agent-demo` (см. `examples/devops_agent_examples.py`).

---

## 📋 Интеграция в пайплайны

| Сценарий | Команда / API | Результат |
|----------|---------------|-----------|
| Локальная генерация YAxUnit | `scripts/tests/generate_bsl_tests.py --module path` | Готовые процедуры тестов |
| QA review в CI | `python -m pytest tests/unit/test_ai_agents.py -k qa_agent` | Smoke-проверка генератора |
| DevOps pipeline | GitHub Actions → шаг `qa-generate-tests` | BDD/negative сценарии сохраняются в artefacts |

---

## 🔐 Безопасность и аудит

- Весь ввод проходит через `StructuredLogger`, чувствительные поля не логируются.
- Параметр `module_type` задаёт уровень доступа (client/server/common).
- В корпоративном режиме выражения отправляются через LLM Gateway с фильтрами.

---

## ✅ Checklist перед релизом

1. Актуализировать `tests/unit/test_ai_agents.py::test_qa_agent_test_generation`.
2. Проверить, что шаблоны соответствуют стандартам команды QA.
3. Для BDD сгенерировать `.feature` файлы и прогнать `vanessa-runner`.
4. Настроить fallback‑цепочку LLM (`config/llm_providers.yaml` → раздел `qa_engineer`).

---

## 📚 Полезные ссылки

- `docs/03-ai-agents/ALL_ASSISTANTS_IMPLEMENTATION_COMPLETE.md` — общий отчёт по агентам.
- `tests/unit/test_ai_agents.py` — живые примеры вызова `generate_tests`.
- `docs/06-features/TESTING_GUIDE.md` — карта тестовых практик.


