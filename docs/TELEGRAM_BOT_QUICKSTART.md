# 🤖 Telegram Bot - Quick Start

## Быстрый старт (5 минут)

### 1️⃣ Получите токен бота

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте `/newbot`
4. Придумайте название: `My 1C AI Assistant`
5. Username: `my_1c_ai_bot` (должен заканчиваться на `_bot`)
6. **Скопируйте токен!** Выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### 2️⃣ Настройте окружение

```bash
# Скопируйте example config
cp .env.example .env

# Откройте .env и вставьте токен
nano .env  # или любой редактор
```

В файле `.env`:
```bash
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### 3️⃣ Запустите бота

**Вариант A: Локально (для разработки)**

```bash
# Установите зависимости
pip install -r requirements.txt
pip install -r requirements-telegram.txt

# Запустите
python -m src.telegram.bot
```

**Вариант B: Docker (рекомендуется)**

```bash
# Запустите всю инфраструктуру + бота
docker-compose -f docker-compose.yml -f docker-compose.stage1.yml --profile telegram up -d

# Проверьте логи
docker logs -f 1c-ai-telegram-bot
```

### 4️⃣ Проверка

1. Откройте Telegram
2. Найдите вашего бота: `@my_1c_ai_bot`
3. Отправьте `/start`
4. Должны получить приветствие! 🎉

---

## 📝 Основные команды

### Для пользователей:

```
/start          - Начать работу
/help           - Справка по командам
/search <запрос> - Поиск кода
/generate <описание> - Генерация кода
/deps <модуль> <функция> - Анализ зависимостей
/stats          - Ваша статистика
/premium        - Информация о Premium
```

### Примеры использования:

**Поиск кода:**
```
/search расчет НДС
/search проведение документа продажи
```

**Генерация кода:**
```
/generate функция расчета скидки по объему
/generate процедура отправки email уведомления
```

**Анализ зависимостей:**
```
/deps РасчетыСервер РассчитатьНДС
/deps ОбщегоНазначения ПустаяДата
```

**Естественные вопросы:**
Просто напишите вопрос без команды:
```
Где в коде мы работаем с налогами?
Как реализована отправка документов?
Покажи все места, где используется регистр Продажи
```

---

## ⚙️ Конфигурация

### Rate Limiting

По умолчанию:
- **10 запросов/минуту** на пользователя
- **100 запросов/день** на пользователя

Изменить в `.env`:
```bash
TELEGRAM_RATE_LIMIT_MIN=20  # запросов в минуту
TELEGRAM_RATE_LIMIT_DAY=500 # запросов в день
```

### Admin права

Узнайте свой Telegram ID через [@userinfobot](https://t.me/userinfobot)

В `.env`:
```bash
TELEGRAM_ADMIN_IDS=123456789,987654321
```

### Premium пользователи

Безлимитный доступ для определенных пользователей:
```bash
TELEGRAM_PREMIUM_IDS=123456789,111222333
```

---

## 🚀 Production Deployment

### Webhook вместо Polling

Для production лучше использовать webhooks:

1. **Получите домен с SSL** (бесплатно: Let's Encrypt)

2. **Настройте nginx** как reverse proxy:
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    location /telegram/webhook {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

3. **В .env укажите webhook:**
```bash
TELEGRAM_WEBHOOK_URL=https://your-domain.com
TELEGRAM_WEBHOOK_PATH=/telegram/webhook
```

4. **Перезапустите бота** - он автоматически зарегистрирует webhook

---

## 📊 Мониторинг

### Логи

```bash
# Docker
docker logs -f 1c-ai-telegram-bot

# Локально - логи в консоли
```

### Метрики

Бот логирует:
- ✅ Успешные запросы
- ❌ Ошибки
- ⏱️ Время обработки
- 👤 ID пользователей
- 📊 Статистика команд

### Healthcheck

Docker автоматически проверяет здоровье бота каждые 30 секунд.

---

## 🔒 Безопасность

### Best Practices:

1. **Храните токен в .env**, не коммитьте в git!
```bash
# Добавлено в .gitignore
.env
```

2. **Rate limiting** защищает от спама

3. **Admin ID** - только админы видят системные команды

4. **Валидация входных данных** - защита от инъекций

5. **Error handling** - пользователи не видят системных ошибок

---

## 🐛 Troubleshooting

### Бот не запускается

**Проблема:** `TELEGRAM_BOT_TOKEN not set!`

**Решение:** Проверьте `.env` файл, токен должен быть без пробелов

---

**Проблема:** `Conflict: terminated by other getUpdates request`

**Решение:** Бот уже запущен в другом месте. Остановите все инстансы:
```bash
docker stop 1c-ai-telegram-bot
# или просто закройте терминал где запускали локально
```

---

**Проблема:** Бот не отвечает на команды

**Решение:** 
1. Проверьте логи: `docker logs 1c-ai-telegram-bot`
2. Убедитесь что БД запущены: `docker ps | grep 1c-ai`
3. Проверьте rate limit: отправьте `/stats`

---

### База данных не подключается

**Проблема:** `Connection refused` к PostgreSQL/Neo4j

**Решение:** Убедитесь что вся инфраструктура запущена:
```bash
docker-compose -f docker-compose.yml -f docker-compose.stage1.yml up -d
```

Проверьте здоровье сервисов:
```bash
docker ps --format "table {{.Names}}\t{{.Status}}"
```

Все сервисы должны быть `healthy` или `Up`.

---

### Бот медленно отвечает

**Возможные причины:**
1. Ollama модель не загружена
2. Qdrant индексы не созданы
3. Rate limit на стороне AI сервисов

**Решение:**
```bash
# Проверьте логи всех сервисов
docker-compose logs ollama qdrant neo4j

# Убедитесь что модель загружена
docker exec 1c-ai-ollama ollama list
```

---

## 📚 Дополнительные ресурсы

- [Aiogram Documentation](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather Commands](https://core.telegram.org/bots#6-botfather)

---

## 💡 Tips & Tricks

### Красивое описание бота

Настройте через @BotFather:

```
/setdescription
[выберите бота]
AI-помощник для 1С разработчиков. 
Семантический поиск, генерация кода, анализ зависимостей.
```

### Команды в меню

```
/setcommands
[выберите бота]

start - Начать работу
search - Поиск кода
generate - Генерация BSL кода
deps - Анализ зависимостей
stats - Статистика
premium - Premium подписка
help - Справка
```

### Аватар бота

```
/setuserpic
[выберите бота]
[загрузите изображение]
```

Рекомендация: 512x512px PNG с логотипом 1С + AI

---

## 🎯 Next Steps

1. ✅ Бот запущен
2. 📝 Настройте команды через @BotFather
3. 👥 Добавьте бота в групповой чат команды
4. 📊 Настройте мониторинг (Prometheus + Grafana)
5. 💎 Реализуйте Premium подписку (Telegram Stars)
6. 🌐 Настройте webhook для production
7. 📱 Создайте web dashboard для статистики

---

**Готово! Ваш Telegram бот работает!** 🚀

Вопросы? Проблемы? → Создайте [Issue на GitHub](../../issues)


