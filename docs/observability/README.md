# 📡 Observability

Инструкции и артефакты для мониторинга 1C AI Stack.

## Основные документы
| Файл | Назначение |
|------|------------|
| [`SLO.md`](SLO.md) | Целевые показатели доступности и задержек, формулы расчёта, ответственность команд. |

## Быстрые действия
- **Развернуть локальный стек наблюдаемости:** `make observability-up` → собирает Prometheus + Grafana + Alertmanager (см. `observability/docker-compose.observability.yml`).
- **Установить в Kubernetes:** `make helm-observability` → устанавливает чарты из `infrastructure/helm/observability-stack`.
- **Проверить алерты:** workflow `.github/workflows/observability.yml` напоминает регулярно обновлять настройки.

## Что ещё посмотреть
- Конфигурация Alertmanager и правила: `observability/alertmanager.yml`, `observability/alerts.yml`.
- Runbook на нарушение SLO: [`docs/runbooks/alert_slo_runbook.md`](../runbooks/alert_slo_runbook.md).
- История DORA-метрик: [`docs/status/dora_history.md`](../status/dora_history.md).

## Как обновлять
1. Вносите изменения в соответствующие YAML/MD файлы.
2. Прогоняйте `make observability-test` (если добавлен) или запуск локального docker-compose.
3. Обновляйте ссылки здесь, чтобы коллеги видели актуальные инструкции.
