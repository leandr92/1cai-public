# 🐍 Python 3.11 Setup Guide

Руководство поможет быстро установить и зафиксировать Python 3.11.x на рабочих станциях.

---

## 🔎 Проверка текущей версии

```bash
python --version
# Ожидаем: Python 3.11.x
```

Если отображается 3.12/3.13/3.14 или более старая версия, выполните шаги ниже.

---

## 🪟 Windows 10/11

### Вариант 1: Официальный установщик (рекомендуется)
1. Скачайте **Windows installer (64-bit)** с [python.org/downloads](https://www.python.org/downloads/release/python-3119/).
2. При установке отметьте опцию **“Add Python 3.11 to PATH”**.
3. После установки перезапустите терминал и проверьте `python --version`.

### Вариант 2: Winget
```powershell
winget install Python.Python.3.11
```

### Несколько версий на одной машине
- Установите [pyenv-win](https://github.com/pyenv-win/pyenv-win):
  ```powershell
  # PowerShell (Administrator)
  Invoke-WebRequest -UseBasicParsing -Uri https://pyenv.win/install.ps1 | Invoke-Expression
  pyenv install 3.11.9
  pyenv global 3.11.9
  ```
- Для каждого проекта используйте виртуальное окружение: `python -m venv venv` → `venv\Scripts\activate`.

---

## 🍎 macOS 12+

```bash
brew install python@3.11
brew link python@3.11 --force
python3.11 -m venv venv
```

Если установлен pyenv:
```bash
brew install pyenv
pyenv install 3.11.9
pyenv local 3.11.9
```

Добавьте `eval "$(pyenv init --path)"` в `~/.zprofile` и перезапустите терминал.

---

## 🐧 Linux (Ubuntu/Debian)

```bash
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt update
sudo apt install python3.11 python3.11-venv python3.11-dev
python3.11 -m venv venv
```

Для Fedora/RHEL:
```bash
sudo dnf install python3.11 python3.11-devel
```

В WSL используйте те же команды, что и для Linux-дистрибутива.

---

## 🧪 Виртуальные окружения

```bash
# Создать venv в каталоге проекта
python3.11 -m venv venv

# Windows
venv\Scripts\activate

# Linux / macOS / WSL
source venv/bin/activate

# Проверка
python --version  # Python 3.11.x
```

### Быстрый сброс окружения
```bash
deactivate  # выйти из venv
rm -rf venv
python3.11 -m venv venv
```

---

## ❗️ Почему именно 3.11

- Стабильная поддержка всеми зависимостями (`asyncpg`, `psycopg2-binary`, `redis`, `fastapi`).
- Поддержка ветки upstream (до 2027 года) + LTS-совместимость.
- На Python 3.12/3.13/3.14 часть библиотек пока не имеет готовых wheel’ов → установки из исходников ломаются.

---

## 🔄 Переключение между версиями

| Инструмент  | Команда                                                                |
|-------------|-------------------------------------------------------------------------|
| `pyenv`     | `pyenv global 3.11.9` или `pyenv local 3.11.9`                         |
| `pyenv-win` | `pyenv global 3.11.9`                                                   |
| venv        | `deactivate` → активировать другое окружение                          |

Всегда перепроверяйте `python --version` внутри активного окружения.

---

## 🧹 Проблемы и решения

| Симптом                                        | Решение                                                                              |
|------------------------------------------------|----------------------------------------------------------------------------------------|
| `ModuleNotFoundError: No module named 'asyncpg'` | Вы используете неправильную версию Python. Переключитесь на 3.11.x и переустановите зависимости. |
| `pip install` пытается собирать wheel из исходников | Убедитесь, что работаете под 3.11 и используете актуальные бинарные колёса.             |
| VSCode/IDE запускает Python 3.14                | В настройках IDE выберите интерпретатор из venv с Python 3.11.x.                       |

---

## ✅ Контрольный список

- [ ] `python --version` → `Python 3.11.x`
- [ ] Создано и активировано виртуальное окружение `venv`
- [ ] Установлены зависимости: `pip install -r requirements.txt`
- [ ] Тесты запускаются: `python -m pytest --no-cov tests/unit/...`
- [ ] IDE использует тот же интерпретатор

После выполнения пунктов проект готов к развитию и деплою.

