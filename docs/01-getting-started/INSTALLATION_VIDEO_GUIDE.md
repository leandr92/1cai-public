# 🎬 Скринкаст: установка и первый запуск 1C AI Stack

> Этот документ — подробный сценарий и раскадровка для записи видео.  
> Итоговый ролик следует опубликовать на YouTube и добавить ссылку в README.

---

## 1. Подготовка к записи

1. Включите захват экрана (1920×1080, 60 fps).  
2. Подготовьте чистую рабочую директорию `C:\demo\1cai-demo`.  
3. Очистите Docker контейнеры (`docker ps -aq | % { docker rm -f $_ }`).  
4. Проверьте, что GitHub Discussions отключены — упомяните, где задавать вопросы (Issues/Telegram).

---

## 2. План ролика (10 минут)

| Таймкод | Действие | Комментарии/скрипт |
|--------|----------|---------------------|
| 00:00-00:40 | Приветствие, цель видео | «Покажу установку и первый запуск 1C AI Stack, куда обращаться, если что-то не работает.» |
| 00:40-01:40 | Клонирование репозитория | `git clone https://github.com/DmitrL-dev/1cai-public.git` → `cd 1cai-public` |
| 01:40-03:00 | Настройка `.env` | `copy env.example .env`, открыть Notepad, заполнить `POSTGRES_PASSWORD`, упомянуть прочие переменные. |
| 03:00-04:30 | Виртуальное окружение и зависимости | `python -m venv .venv`, `.\.venv\Scripts\activate`, `pip install -r requirements.txt` |
| 04:30-06:00 | Запуск Docker-сервисов | `docker-compose up -d`, `docker-compose ps`, `docker-compose run --rm migrations` |
| 06:00-07:30 | Проверка сервисов | `docker-compose exec postgres pg_isready`, открытие http://localhost/health |
| 07:30-08:30 | Семантический поиск (пример) | `python examples/query_semantic_search.py "как рассчитать налог"` |
| 08:30-09:30 | Куда задавать вопросы | Показать README → новый раздел SUPPORT, сослаться на Issues/Telegram. |
| 09:30-10:00 | Финал | «Скринкаст завершён, ссылка в README и Quick Start.» |

---

## 3. После записи

1. Сохраните файл как `installation-demo.mp4`.  
2. Загрузите в раздел «Releases» или на YouTube.  
3. Добавьте ссылку в README и QUICKSTART (`▶ Видео-демо`).  
4. Ответьте в issue [#3](https://github.com/DmitrL-dev/1cai-public/issues/3) ссылкой на ролик.

---

## 4. Шаблон описания (YouTube)

```
Название: 1C AI Stack — Установка и первый запуск (10 минут)
Описание:
- Репозиторий: https://github.com/DmitrL-dev/1cai-public
- Документация: docs/01-getting-started/README.md
- FAQ и поддержка: docs/SUPPORT.md

0:00 Введение
0:40 Клонирование
1:40 Настройка .env
3:00 Виртуальное окружение
4:30 Docker Compose
6:00 Проверка сервисов
7:30 Семантический поиск
8:30 Где задавать вопросы
9:30 Завершение
```

---

## 5. FAQ для видео

- **Где задавать вопросы?** → GitHub Issues или Telegram-чат (`docs/SUPPORT.md`).  
- **Почему Discussions недоступны?** → временно отключены; укажите, что альтернатива описана в README.  
- **Можно ли без Docker?** → коротко объяснить, что продвинутые сценарии есть в документации, но для демо достаточно Docker.

---

Готово: осталось записать скринкаст по сценарию и добавить ссылку в документацию. Когда ролик опубликован, не забудьте закрыть issue #3.

