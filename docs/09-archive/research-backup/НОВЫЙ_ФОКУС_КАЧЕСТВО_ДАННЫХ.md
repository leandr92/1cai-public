# 🎯 НОВЫЙ ФОКУС: Качество данных для обучения моделей

**Дата:** 2025-11-05  
**Реальная проблема:** Не скорость, а КАЧЕСТВО данных  
**Цель:** Продукт который опередит конкурентов

---

## ✅ ПОНЯЛ ПРАВИЛЬНО

### Реальные вопросы:

1. **Источник данных:** ✅ EDT выгрузка (config.xml), одной конфигурации хватит
2. **Проблема:** ❌ НЕ скорость парсинга
3. **Проблема:** ✅ КАЧЕСТВО И ПОЛНОТА данных для обучения
4. **Цель:** Создать опережающий продукт

---

## 🔍 КРИТИЧЕСКИЕ ВОПРОСЫ ДЛЯ ОБУЧЕНИЯ МОДЕЛЕЙ

### Что ДЕЙСТВИТЕЛЬНО нужно извлекать?

#### 1. Контекст функции (не только код!)

**Текущий парсер извлекает:**
```python
{
    'name': 'ПолучитьДанные',
    'code': 'Функция ПолучитьДанные()...',
    'params': ['Параметр1', 'Параметр2']
}
```

**Что РЕАЛЬНО нужно для обучения модели:**
```python
{
    'name': 'ПолучитьДанные',
    'code': '...',
    'params': [...],
    
    # КРИТИЧНО для обучения:
    'context': {
        'object_type': 'Document.Invoice',  # Контекст объекта
        'purpose': 'Получение данных накладной',  # Бизнес-логика
        'related_functions': ['ОбработатьДанные', 'СохранитьДанные'],  # Связи
        'used_by': ['ПроцедураПроведения', 'ФормаДокумента'],  # Где используется
        'uses_api': ['Справочники.Номенклатура', 'Запрос'],  # Какие API
    },
    
    # Примеры использования
    'usage_examples': [
        'Данные = ПолучитьДанные(ДокументСсылка);'
    ],
    
    # Метаинформация
    'metadata': {
        'is_server': True,
        'is_client': False,
        'region': 'ПрограммныйИнтерфейс',
        'complexity': 'medium',
        'best_practice_compliance': 0.85
    }
}
```

**ВОПРОС:** Что из этого текущий парсер НЕ извлекает?

---

#### 2. Связи между функциями (граф вызовов)

**Для хорошего обучения нужно знать:**
- Какие функции вызываются вместе
- Какие паттерны использования
- Зависимости

**Текущий парсер:** Извлекает функции независимо ❌

**Нужно:** Построить граф вызовов ✅

---

#### 3. Типовые паттерны кода

**Для генерации нужно знать:**
- Как обычно пишут CRUD операции
- Какие паттерны для работы с формами
- Какие конструкции для запросов

**Текущий парсер:** Не извлекает паттерны ❌

**Нужно:** Pattern extraction ✅

---

#### 4. Бизнес-контекст

**Модель должна понимать:**
- Это функция для расчета налогов
- Это для работы с документами
- Это для интеграции

**Текущий парсер:** Только имя функции ❌

**Нужно:** Semantic labeling ✅

---

## 🎯 ПЛАН УЛУЧШЕНИЯ КАЧЕСТВА ДАННЫХ

### Phase 1: Анализ EDT выгрузки (1-2 дня)

**Задачи:**
1. Получить ваш config.xml
2. Изучить структуру EDT выгрузки
3. Понять что ЕСТЬ в файле
4. Понять что парсер НЕ извлекает

**Deliverable:** Список недостающей информации

---

### Phase 2: Улучшение извлечения данных (3-5 дней)

**На основе анализа Phase 1, добавить извлечение:**

1. **Метаданные объектов**
   ```python
   - Свойства документа/справочника
   - Реквизиты и их типы
   - Табличные части
   ```

2. **Граф зависимостей**
   ```python
   - Какая функция вызывает какую
   - Data flow между функциями
   - Зависимости от API
   ```

3. **Паттерны использования**
   ```python
   - Типовые конструкции
   - Best practices примеры
   - Anti-patterns
   ```

4. **Бизнес-контекст**
   ```python
   - Категоризация функций
   - Определение purpose
   - Domain knowledge
   ```

---

### Phase 3: Обогащение dataset (2-3 дня)

**Создать enriched dataset:**
```python
{
    'code': '...',
    'metadata': {...},     # Полная метаинформация
    'context': {...},      # Бизнес-контекст
    'dependencies': [...], # Граф вызовов
    'patterns': [...],     # Используемые паттерны
    'quality_score': 0.85, # Оценка качества
    
    # ДЛЯ ОБУЧЕНИЯ:
    'instruction': 'Создай функцию получения данных документа',
    'input': 'Параметры: ДокументСсылка',
    'output': '...',  # Код
    
    # КОНТЕКСТ для модели:
    'similar_examples': [...],  # Похожие функции
    'best_practices': [...],    # Рекомендации
}
```

---

### Phase 4: Оптимизация обучения (1 неделя)

**Улучшить процесс обучения локальных моделей:**

1. **Structured training data**
   - Не просто код, а код + контекст + метаданные
   
2. **Категоризация примеров**
   - CRUD отдельно
   - Forms отдельно
   - Queries отдельно
   - Модель учится на структурированных данных

3. **Quality filtering**
   - Только хорошие примеры (best practices)
   - Исключить anti-patterns
   - Модель учится на качественном коде

4. **Fine-tuning strategy**
   - Multi-task learning (код + intent + качество)
   - Curriculum learning (от простого к сложному)
   - Специализированные веса для BSL синтаксиса

---

## 🔬 КОНКРЕТНЫЕ ВОПРОСЫ ДЛЯ АНАЛИЗА

### Про текущий парсер:

1. **Что он извлекает СЕЙЧАС?**
   ```
   Проверить на вашем config.xml:
   - Сколько функций находит?
   - Всё ли извлекается?
   - Какая информация теряется?
   ```

2. **Что НУЖНО извлекать дополнительно?**
   ```
   Для обучения моделей критично:
   - Контекст объекта?
   - Связи между функциями?
   - Метаданные реквизитов?
   - Что еще?
   ```

3. **Какие проблемы в обучении моделей?**
   ```
   Сейчас:
   - Модель генерирует код точность X%
   - Какие ошибки делает чаще всего?
   - Чего не хватает в training data?
   ```

---

## 🎯 ACTION PLAN

### Immediate (сегодня-завтра):

1. **Получить ваш config.xml из EDT**
2. **Запустить текущий парсер на нем**
3. **Проанализировать:**
   - Что извлекается?
   - Что теряется?
   - Что нужно добавить?

### Short-term (неделя):

4. **Улучшить парсер для извлечения:**
   - Метаданных объектов
   - Связей между функциями
   - Бизнес-контекста
   - Всего что нужно для обучения

5. **Создать enriched dataset:**
   - Код + контекст + метаданные
   - Quality filtering
   - Категоризация

### Medium-term (2-4 недели):

6. **Оптимизировать обучение моделей:**
   - Structured training
   - Multi-task learning
   - Quality-focused approach

7. **Измерить улучшение:**
   - Точность генерации до/после
   - Какие ошибки исправлены
   - Реальное улучшение качества

---

## 💡 КОНКУРЕНТНОЕ ПРЕИМУЩЕСТВО

### Что сделает нас уникальными:

**НЕ скорость парсинга** (все достаточно быстрые)

**А:**
1. ✅ **Полнота извлекаемых данных**
   - Метаданные объектов
   - Граф зависимостей
   - Бизнес-контекст

2. ✅ **Качество training dataset**
   - Не просто код, а enriched data
   - Context-aware examples
   - Best practices focus

3. ✅ **Эффективность обучения**
   - Structured learning
   - Multi-task approach
   - Domain-specific fine-tuning

**Результат:** Модель которая ПОНИМАЕТ 1С лучше конкурентов!

---

## 🚀 СЛЕДУЮЩИЙ ШАГ

**Пришлите config.xml (или укажите где взять)**

Я:
1. Запущу парсер
2. Проанализирую что извлекается
3. Найду что теряется
4. Предложу конкретные улучшения

**Фокус на КАЧЕСТВЕ данных для обучения, не на скорости парсинга.**

Готов приступить! 🎯


