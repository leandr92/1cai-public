# Система метрик и мониторинга блокировок Rate Limiting

Основана на стандартах мониторинга 1С:Предприятие (Центр управления производительностью - ЦУП)

## Обзор

Система мониторинга обеспечивает комплексный контроль за блокировками Rate Limiting с поддержкой:

- ✅ **Сбор метрик** - детализация по IP, пользователю, MCP tool
- ✅ **Prometheus экспорт** - PromQL совместимые метрики
- ✅ **Система алертов** - real-time уведомления при превышении порогов
- ✅ **Grafana интеграция** - готовые дашборды для визуализации
- ✅ **Real-time мониторинг** - мониторинг в реальном времени
- ✅ **Health checks** - проверка состояния всех компонентов
- ✅ **Distributed tracing** - поддержка распределенного трейсинга

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│              RateLimitMonitoringSystem                  │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │ RateLimitMetrics│  │  AlertManager   │              │
│  │     Collector   │  │     (Alerts)    │              │
│  └─────────────────┘  └─────────────────┘              │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐              │
│  │ Prometheus      │  │ RealTimeMonitor │              │
│  │    Exporter     │  │   (Callbacks)   │              │
│  └─────────────────┘  └─────────────────┘              │
│                                                         │
│  ┌─────────────────┐                                    │
│  │ RateLimitDashboard│                                   │
│  │   (Grafana)     │                                    │
│  └─────────────────┘                                    │
└─────────────────────────────────────────────────────────┘
```

## Основные компоненты

### 1. RateLimitMetrics
Сборщик метрик блокировок с поддержкой:
- **Счетчики**: общее количество запросов, заблокированные запросы
- **Показатели**: RPS по IP/пользователю, активные ограничения
- **Гистограммы**: время обработки лимитов
- **Детализация**: по IP, user_id, MCP tool

### 2. PrometheusExporter
Экспорт метрик в формате Prometheus с поддержкой:
- PromQL совместимые имена метрик
- Автоматический экспорт в файл
- Интеграция с existing monitoring stack

### 3. AlertManager
Система алертов при превышении порогов:
- **Уровни критичности**: INFO, WARNING, CRITICAL, EMERGENCY
- **Настраиваемые правила**: условия, пороги, длительность
- **Real-time алертинг**: мгновенные уведомления
- **История алертов**: отслеживание срабатываний

### 4. RateLimitDashboard
Генерация данных для Grafana дашбордов:
- Готовые панели для основных метрик
- PromQL запросы для дашбордов
- Конфигурация Grafana в JSON формате

### 5. RealTimeMonitor
Мониторинг в реальном времени:
- **Callbacks**: уведомления об алертах и метриках
- **Health checks**: проверка состояния системы
- **Statistics**: статистика мониторинга
- **Threading**: асинхронная работа

## Метрики

### Основные метрики

```prometheus
# Общее количество запросов
rate_limit_requests_total

# Количество заблокированных запросов  
rate_limit_blocked_total

# RPS по IP/пользователю/инструменту
rate_limit_requests_per_second{ip="192.168.1.1", user_id="user123", tool="search"}

# Количество активных ограничений
rate_limit_active_limits

# Время обработки лимитов (гистограмма)
rate_limit_response_time_seconds

# Текущее количество заблокированных
rate_limit_blocked_current

# Статус здоровья системы
rate_limit_health_status
```

### PromQL запросы

```prometheus
# Скорость запросов в минуту
rate(rate_limit_requests_total[1m])

# Процент заблокированных запросов
(rate(rate_limit_blocked_total[5m]) / rate(rate_limit_requests_total[5m])) * 100

# 95-й перцентиль времени ответа
histogram_quantile(0.95, rate(rate_limit_response_time_seconds_bucket[5m]))

# Топ IP по RPS
topk(10, rate_limit_requests_per_second)

# Количество активных ограничений
rate_limit_active_limits
```

## Установка и использование

### Базовое использование

```python
from ratelimit.metrics import RateLimitMonitoringSystem

# Создание системы мониторинга
monitoring = RateLimitMonitoringSystem(
    metrics_history_size=10000,
    monitoring_interval=1,
    enable_prometheus_export=True,
    enable_realtime_monitoring=True
)

# Запуск системы
monitoring.start()

# Запись метрик запроса
monitoring.record_request(
    ip="192.168.1.100",
    user_id="user123",
    tool="search",
    response_time=0.150,
    blocked=False
)

# Регистрация активного ограничения
monitoring.register_limit(
    "limit_123",
    {"type": "rate_limit", "limit": 100, "window": 60}
)

# Получение метрик
summary = monitoring.metrics_collector.get_metrics_summary()
print(f"Всего запросов: {summary['total_requests']}")

# Остановка системы
monitoring.stop()
```

### Настройка пользовательских алертов

```python
from ratelimit.metrics import AlertRule, AlertSeverity
from datetime import timedelta

# Создание правила алерта
high_error_rule = AlertRule(
    name="high_error_rate",
    metric_name="rate_limit_summary_blocked_rate",
    condition=">",
    threshold=0.05,  # 5% ошибок
    severity=AlertSeverity.WARNING,
    duration=timedelta(minutes=2),
    description="Высокий процент заблокированных запросов"
)

# Добавление правила
monitoring.add_custom_alert_rule(high_error_rule)
```

### Использование декоратора

```python
from ratelimit.metrics import rate_limit_monitoring

@rate_limit_monitoring(monitoring_system)
def api_function(ip: str, user_id: str, tool: str = "api_call"):
    # Автоматический мониторинг всех вызовов
    return {"status": "success", "data": "result"}

# Использование
result = api_call(ip="192.168.1.100", user_id="user123")
```

### Real-time callbacks

```python
# Callback для алертов
def alert_callback(alerts):
    for alert in alerts:
        print(f"🚨 Alert: {alert['rule_name']} - {alert['description']}")

monitoring.realtime_monitor.add_alert_callback(alert_callback)

# Callback для метрик
def metrics_callback(stats):
    print(f"RPS: {stats['peak_rps']:.1f}, "
          f"Response time: {stats['avg_response_time']:.3f}s")

monitoring.realtime_monitor.add_metrics_callback(metrics_callback)
```

### Экспорт в Prometheus

```python
# Экспорт в файл
monitoring.export_prometheus_metrics('/tmp/rate_limit_metrics.prom')

# Получение метрик как строки
prometheus_text = monitoring.export_prometheus_metrics()
print(prometheus_text)
```

### Генерация Grafana дашборда

```python
# Экспорт конфигурации дашборда
monitoring.export_grafana_dashboard('/tmp/dashboard.json')

# Получение PromQL запросов
queries = monitoring.get_prometheus_queries()
for name, query in queries.items():
    print(f"{name}: {query}")
```

## Health Checks

```python
# Принудительная проверка здоровья
health = monitoring.realtime_monitor.force_health_check()
print(f"System health: {health['overall_health']}")

# Проверка компонентов
for check, result in health['checks'].items():
    status = "✓" if result else "✗"
    print(f"{status} {check}")

# Получение статуса системы
status = monitoring.get_system_status()
print(f"Uptime: {status['uptime_seconds']:.1f} seconds")
print(f"Active alerts: {status['components']['alert_manager']['active_alerts']}")
```

## Предопределенные правила алертов

Система включает набор предопределенных правил алертов:

1. **high_block_rate** - высокий процент заблокированных запросов (>10%)
2. **excessive_rps** - превышение лимита RPS (>100 RPS с IP)
3. **slow_response** - медленное время отклика (>1 сек)
4. **system_unhealthy** - система мониторинга нездорова
5. **too_many_active_limits** - слишком много активных ограничений (>1000)

## Интеграция с существующим мониторингом

### Prometheus

```yaml
# prometheus.yml
scrape_configs:
  - job_name: 'rate-limiting'
    static_configs:
      - targets: ['localhost:8000']
    metrics_path: '/metrics'
    scrape_interval: 15s
```

### Grafana

Импортируйте сгенерированную конфигурацию дашборда в Grafana или используйте предоставленные PromQL запросы.

### Alertmanager

```yaml
# alertmanager.yml
route:
  group_by: ['alertname']
  receiver: 'web.hook'

receivers:
  - name: 'web.hook'
    webhook_configs:
      - url: 'http://127.0.0.1:5001/'
```

## Производительность

Система оптимизирована для высокой производительности:

- **Thread-safe**: все операции защищены мьютексами
- **Memory efficient**: ограниченный размер истории метрик
- **Async callbacks**: неблокирующие уведомления
- **Lazy loading**: метрики собираются по требованию

### Бенчмарки

```
Сбор метрик: ~10,000 метрик/сек
Экспорт Prometheus: ~1,000 метрик/сек  
Проверка алертов: ~1,000 проверок/сек
Real-time мониторинг: обновление каждые 1 сек
```

## Примеры

Запустите файл с примерами:

```bash
python ratelimit/monitoring_examples.py
```

Примеры включают:
- Базовый мониторинг
- Пользовательские алерты
- Экспорт в Prometheus
- Генерация Grafana дашбордов
- Декоратор мониторинга
- Health checks

## Логирование

Система использует Python logging с уровнями:

- **INFO**: общая информация о работе системы
- **WARNING**: предупреждения и срабатывание алертов
- **ERROR**: ошибки в работе компонентов
- **CRITICAL**: критические проблемы системы

```python
import logging
logging.basicConfig(level=logging.INFO)
```

## Расширение

### Добавление пользовательских метрик

```python
# Добавление кастомной метрики
monitoring.metrics_collector._record_gauge(
    'custom_metric_name',
    {'label': 'value'},
    42.0
)
```

### Создание пользовательских алертов

```python
custom_rule = AlertRule(
    name="custom_alert",
    metric_name="custom_metric_name",
    condition=">",
    threshold=100,
    severity=AlertSeverity.CRITICAL
)

monitoring.add_custom_alert_rule(custom_rule)
```

### Интеграция с внешними системами

```python
def custom_alert_handler(alerts):
    # Отправка в Slack, email, etc.
    for alert in alerts:
        send_to_slack(alert)

monitoring.realtime_monitor.add_alert_callback(custom_alert_handler)
```

## Troubleshooting

### Проблемы с памятью

Если система использует слишком много памяти:
- Уменьшите `metrics_history_size`
- Увеличьте интервал мониторинга
- Отключите real-time мониторинг

### Высокая нагрузка CPU

Для снижения нагрузки:
- Увеличьте `monitoring_interval`
- Уменьшите частоту callback'ов
- Отключите детальное логирование

### Проблемы с экспортом

Если экспорт в Prometheus не работает:
- Проверьте права записи в файл
- Убедитесь что директория существует
- Проверьте формат метрик

## Лицензия

Модуль создан на основе стандартов производительности 1С:Предприятие и предназначен для использования в системах мониторинга Rate Limiting.

## Поддержка

Для получения поддержки и сообщения об ошибках создавайте issues в репозитории проекта.
