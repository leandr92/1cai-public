apiVersion: 1

providers:
  - name: 'MCP Server Dashboards'
    orgId: 1
    folder: 'MCP Server'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/mcp_server

dashboards:
  - name: 'MCP Server Overview'
    folder: 'MCP Server'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/mcp_server

  - name: 'MCP Operations Dashboard'
    folder: 'MCP Server'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/mcp_server

  - name: 'MCP Error Analysis'
    folder: 'MCP Server'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards/mcp_server