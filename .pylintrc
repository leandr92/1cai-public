[MASTER]
# Files or directories to be skipped (regex patterns)
ignore=
    src/deployment/orchestration.py,
    src/gbnf_generator/bsl_grammar_rules.py

# Reason: These files contain syntax that pylint cannot parse correctly:
# - orchestration.py:430 - complex f-string with Prometheus query syntax
# - bsl_grammar_rules.py:196 - grammar pattern definition with special characters
# Both files are syntactically valid Python but trigger false positive parsing errors
