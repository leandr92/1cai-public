# Интеграционные Тесты 1C MCP Code Generation System

Данная директория содержит комплексную систему интеграционных тестов для проверки полного цикла работы системы генерации кода 1C.

## Структура Тестов

### Файлы Тестов

| Файл | Описание | Количество Тестов |
|------|----------|-------------------|
| `conftest.py` | Конфигурация pytest и общие fixtures | - |
| `test_full_generation_cycle.py` | Тесты полного цикла генерации кода | 10 |
| `test_code_validation.py` | Тесты валидации сгенерированного кода | 12 |
| `test_security.py` | Тесты безопасности и защиты | 15 |
| `test_mcp_integration.py` | Тесты интеграции с MCP протоколом | 16 |
| `test_e2e_scenarios.py` | End-to-End сценарии использования | 8 |

### Категории Тестов

#### 🔄 Полный Цикл Генерации (`test_full_generation_cycle.py`)
- **Цель**: Проверка процесса от промпта до готового кода
- **Покрытие**: 
  - Генерация обработок, отчетов, справочников, документов
  - Интеграция с шаблонами и промптами
  - Контекстуальная генерация
  - Параллельная обработка запросов
  - Оптимизация промптов

#### ✅ Валидация Кода (`test_code_validation.py`)
- **Цель**: Проверка качества и соответствия стандартам
- **Покрытие**:
  - Синтаксическая валидация
  - Семантический анализ
  - Соответствие стандартам 1C
  - Проверка производительности
  - Метрики качества кода
  - Пакетная валидация

#### 🔒 Безопасность (`test_security.py`)
- **Цель**: Защита от угроз и уязвимостей
- **Покрытие**:
  - Обнаружение SQL injection
  - Блокировка опасных операций
  - Контроль доступа к файлам
  - Валидация входных данных
  - Аудит событий безопасности
  - Шифрование данных

#### 🔌 MCP Интеграция (`test_mcp_integration.py`)
- **Цель**: Проверка работы с MCP протоколом
- **Покрытие**:
  - Инициализация MCP сервера
  - Инструменты генерации и валидации
  - Обработка запросов и ответов
  - Пакетные операции
  - Управление сессиями
  - Соответствие протоколу

#### 🎯 End-to-End (`test_e2e_scenarios.py`)
- **Цель**: Реальные сценарии использования
- **Покрытие**:
  - Полный workflow генерации
  - Жизненный цикл объектов 1C
  - CLI интерфейс
  - Производительность под нагрузкой
  - Восстановление после ошибок
  - Проверка здоровья системы

## Запуск Тестов

### Базовые Команды

```bash
# Запуск всех интеграционных тестов
pytest tests/integration/ -v

# Запуск конкретного файла тестов
pytest tests/integration/test_full_generation_cycle.py -v

# Запуск с маркировкой
pytest tests/integration/ -m integration -v
pytest tests/integration/ -m security -v
pytest tests/integration/ -m performance -v

# Запуск с детализацией
pytest tests/integration/ -v -s --tb=short
```

### Специализированные Запуски

```bash
# Только быстрые тесты (исключая performance)
pytest tests/integration/ -m "not performance" -v

# Только тесты безопасности
pytest tests/integration/test_security.py -v

# Только MCP тесты с детализацией
pytest tests/integration/test_mcp_integration.py -v -s

# E2E тесты с таймаутом
pytest tests/integration/test_e2e_scenarios.py --timeout=300 -v
```

### Генерация Отчетов

```bash
# HTML отчет о покрытии
pytest tests/integration/ --cov=src --cov-report=html

# JUnit XML для CI/CD
pytest tests/integration/ --junitxml=tests/integration/results.xml

# Подробный отчет о производительности
pytest tests/integration/ -m performance --durations=10
```

## Конфигурация

### Переменные Окружения

```bash
# Уровень логирования
export LOG_LEVEL=INFO

# Путь к временным файлам
export TEMP_DIR=/tmp/1c_mcp_tests

# Настройки производительности
export TEST_TIMEOUT=300
export MAX_PARALLEL_TESTS=5

# Настройки безопасности
export SECURITY_TEST_MODE=strict
export AUDIT_LOG_LEVEL=debug
```

### Настройка Pytest

Создайте `pytest.ini` в корне проекта:

```ini
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
markers =
    integration: интеграционные тесты
    slow: медленные тесты
    security: тесты безопасности
    performance: тесты производительности
    e2e: end-to-end тесты
```

## Mock Объекты и Fixtures

### Основные Fixtures

#### `integration_test_setup`
Полная настройка всех компонентов системы для тестирования:
- Инициализированные компоненты
- Временная директория для файлов
- Конфигурационные файлы
- Mock LLM клиент

#### `sample_generation_requests`
Примеры запросов для генерации различных типов объектов 1C.

#### `sample_1c_code`
Коллекция примеров кода 1C для тестирования:
- Валидный код
- Код с SQL injection
- Опасные операции
- Правильно структурированный код

#### `audit_logger`
Система аудита тестов с записью в лог:
- Время выполнения тестов
- Результаты и метрики
- Ошибки и контекст

### Использование Mock Объектов

```python
# Пример использования mock LLM клиента
@pytest.mark.asyncio
async def test_generation(mock_llm_client):
    # Mock уже настроен с типичными ответами
    engine = CodeGenerationEngine(llm_client=mock_llm_client)
    
    result = await engine.generate_code(
        object_type="processing",
        description="Тестовая обработка"
    )
    
    assert result.success is True
    # Mock гарантирует успешный ответ
```

## Метрики и Аналитика

### Отслеживаемые Метрики

#### Производительность
- Время генерации кода
- Время валидации
- Пропускная способность (запросы/мин)
- Время отклика системы

#### Качество
- Процент успешных тестов
- Покрытие кода
- Стабильность под нагрузкой
- Время восстановления

#### Безопасность
- Обнаруженные угрозы
- Заблокированные операции
- Соответствие политикам безопасности
- Аудит событий

### Интерпретация Результатов

#### ✅ Успешный Запуск
- Все тесты прошли (100% success rate)
- Время выполнения в пределах нормы
- Нет критических ошибок безопасности

#### ⚠️ Предупреждения
- Некоторые тесты не прошли (80-95% success rate)
- Продолжительное время выполнения
- Предупреждения безопасности (не критичные)

#### ❌ Критические Ошибки
- Множественные неудачи тестов (<80% success rate)
- Превышение таймаутов
- Критические уязвимости безопасности
- Система нестабильна

## Интеграция с CI/CD

### GitHub Actions

```yaml
name: Integration Tests
on: [push, pull_request]

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-cov pytest-timeout
    
    - name: Run integration tests
      run: |
        pytest tests/integration/ \
          --junitxml=test-results.xml \
          --cov=src \
          --cov-report=xml
    
    - name: Upload test results
      uses: actions/upload-artifact@v2
      with:
        name: test-results
        path: test-results.xml
```

### Jenkins Pipeline

```groovy
pipeline {
    agent any
    
    stages {
        stage('Integration Tests') {
            steps {
                sh 'pytest tests/integration/ -v --junitxml=results.xml'
            }
            post {
                always {
                    junit 'results.xml'
                    publishHTML([
                        allowMissing: false,
                        alwaysLinkToLastBuild: true,
                        keepAll: true,
                        reportDir: 'htmlcov',
                        reportFiles: 'index.html',
                        reportName: 'Coverage Report'
                    ])
                }
            }
        }
    }
}
```

## Устранение Проблем

### Частые Ошибки

#### ImportError: No module named 'src'
**Решение**: Убедитесь что PYTHONPATH включает корневую директорию проекта
```bash
export PYTHONPATH=$PWD:$PYTHONPATH
```

#### TimeoutError: Test execution timeout
**Решение**: Увеличьте timeout или оптимизируйте тест
```python
@pytest.mark.timeout(300)  # 5 минут
```

#### PermissionError при создании файлов
**Решение**: Проверьте права доступа к временной директории
```bash
chmod 755 /tmp/1c_mcp_tests
```

#### Mock объекты не работают
**Решение**: Проверьте что все необходимые атрибуты mock объекта настроены
```python
mock_client.generate_code.return_value = "expected_result"
```

### Отладка Тестов

```bash
# Включить подробное логирование
pytest tests/integration/ -v -s --log-cli-level=DEBUG

# Запустить конкретный тест с pdb
pytest tests/integration/test_full_generation_cycle.py::test_simple_processing_generation -v -s --pdb

# Проверить производительность
pytest tests/integration/ -m performance --durations=0
```

## Рекомендации

### Написание Новых Тестов

1. **Используйте descriptive names**: `test_should_generate_valid_processing_code`
2. **Добавляйте docstrings**: Описывайте цель и ожидания теста
3. **Маркируйте тесты**: Используйте соответствующие pytest.mark
4. **Изолируйте тесты**: Каждый тест должен быть независимым
5. **Используйте fixtures**: Избегайте дублирования кода

### Поддержка Тестов

- Регулярно обновляйте тесты при изменении API
- Добавляйте новые тесты при обнаружении багов
- Поддерживайте высокое покрытие кода (>90%)
- Мониторьте время выполнения тестов

### Безопасность

- Никогда не используйте реальные API ключи в тестах
- Очищайте временные файлы после тестов
- Проверяйте безопасность mock объектов
- Логируйте все критические события