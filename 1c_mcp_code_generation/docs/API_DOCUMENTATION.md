# API Документация для 1C AI MCP Code Generation System

**Версия:** 1.0  
**Дата создания:** 30.10.2025  

## Содержание

1. [Введение](#введение)
2. [Обзор системы](#обзор-системы)
3. [MCP Инструменты](#mcp-инструменты)
4. [Примеры использования](#примеры-использования)
5. [Руководство по конфигурации](#руководство-по-конфигурации)
6. [Troubleshooting](#troubleshooting)

---

## Введение

1C AI MCP Code Generation System - это интеллектуальная система генерации кода для платформы 1С:Предприятие, использующая современные технологии искусственного интеллекта и MCP (Model Context Protocol) для автоматизации создания высококачественного кода.

### Основные возможности

- 🤖 **Генерация кода с помощью ИИ** - Создание кода 1С на основе описаний на естественном языке
- ✅ **Валидация и проверка качества** - Автоматическая проверка синтаксиса, семантики и стандартов кода
- 🔒 **Проверка безопасности** - Выявление потенциальных уязвимостей и опасных операций
- 📝 **Оптимизация промптов** - Улучшение качества результатов генерации
- 📚 **Библиотека шаблонов** - Готовые шаблоны для различных типов объектов 1С

---

## Обзор системы

### Архитектура

```
┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐
│   MCP Клиент        │────│   MCP Сервер        │────│  1C AI Engine       │
│                     │    │                     │    │                     │
│ • Claude Desktop    │    │ • JSON-RPC 2.0      │    │ • LLM Client        │
│ • VS Code Extension │    │ • Инструменты       │    │ • Template Manager  │
│ • IDE Integration   │    │ • Обработка запросов│    │ • Code Validator    │
└─────────────────────┘    └─────────────────────┘    │ • Security Manager  │
                                                         │ • Audit Logger      │
                                                         └─────────────────────┘
```

### Компоненты системы

1. **CodeGenerationEngine** - Основной движок генерации кода
2. **LLMClient** - Клиент для работы с языковыми моделями
3. **TemplateManager** - Управление библиотекой шаблонов
4. **CodeValidator** - Валидация и проверка качества кода
5. **SecurityManager** - Анализ безопасности
6. **AuditLogger** - Аудит и логирование операций

---

## MCP Инструменты

### 1. generate_code

Генерирует код 1С на основе описания и параметров.

**Эндпоинт:** `tools/call`  
**Название:** `generate_code`

#### Параметры запроса

```json
{
  "object_type": "processing|report|catalog|document|form",
  "description": "Описание функциональности на естественном языке",
  "parameters": {
    "object_name": "ИмяОбъекта",
    "description": "Подробное описание объекта",
    "author": "СистемаСоздания",
    "features": ["список", "функций"]
  }
}
```

#### Описание параметров

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `object_type` | string | Да | Тип создаваемого объекта 1С |
| `description` | string | Да | Описание функциональности |
| `object_name` | string | Нет | Имя создаваемого объекта |
| `features` | array | Нет | Список дополнительных функций |

#### Поддерживаемые типы объектов

- `processing` - Обработки
- `report` - Отчеты
- `catalog` - Справочники
- `document` - Документы
- `form` - Формы
- `enumeration` - Перечисления
- `charlen` - Характеристики

#### Пример запроса

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "generate_code",
    "arguments": {
      "object_type": "processing",
      "description": "Обработка для анализа продаж по периодам с возможностью фильтрации",
      "parameters": {
        "object_name": "АнализПродаж",
        "description": "Обработка для анализа продаж",
        "author": "AI System",
        "features": ["отчетность", "фильтры", "экспорт"]
      }
    }
  }
}
```

#### Ответ

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Сгенерированный код обработки..."
      }
    ],
    "metadata": {
      "generation_time": 2.34,
      "confidence_score": 0.95,
      "object_type": "processing"
    }
  }
}
```

#### Коды ошибок

| Код | Описание |
|-----|----------|
| INVALID_OBJECT_TYPE | Недопустимый тип объекта |
| DESCRIPTION_TOO_SHORT | Описание слишком короткое |
| LLM_GENERATION_FAILED | Ошибка генерации LLM |
| TEMPLATE_NOT_FOUND | Шаблон не найден |

---

### 2. validate_code

Проверяет качество и соответствие стандартам сгенерированного кода.

**Эндпоинт:** `tools/call`  
**Название:** `validate_code`

#### Параметры запроса

```json
{
  "code": "Исходный код для проверки",
  "validation_types": ["syntax", "semantics", "standards", "performance"],
  "strict_mode": false,
  "custom_rules": []
}
```

#### Типы валидации

- `syntax` - Синтаксическая проверка
- `semantics` - Семантическая проверка
- `standards` - Соответствие стандартам 1С
- `performance` - Анализ производительности
- `security` - Проверка безопасности
- `readability` - Читабельность кода

#### Пример запроса

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/call",
  "params": {
    "name": "validate_code",
    "arguments": {
      "code": "&НаСервере\nПроцедура ВыполнитьОбработку() Экспорт\n    Запрос = Новый Запрос;\n    Запрос.Текст = \"ВЫБРАТЬ * ИЗ Справочник.Номенклатура\";\n    Результат = Запрос.Выполнить();\nКонецПроцедуры",
      "validation_types": ["syntax", "semantics", "standards"],
      "strict_mode": true
    }
  }
}
```

#### Ответ

```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Валидация завершена. Найдено 0 ошибок, 1 предупреждение."
      }
    ],
    "validation_results": {
      "is_valid": true,
      "overall_score": 85,
      "issues": [
        {
          "type": "warning",
          "code": "STYLE_001",
          "description": "Рекомендуется добавить комментарии к процедуре",
          "line": 2,
          "severity": "low"
        }
      ],
      "detailed_results": {
        "syntax": {"score": 100, "issues": []},
        "semantics": {"score": 90, "issues": []},
        "standards": {"score": 80, "issues": []}
      }
    }
  }
}
```

---

### 3. security_check

Проверяет код на наличие потенциальных уязвимостей и опасных операций.

**Эндпоинт:** `tools/call`  
**Название:** `security_check`

#### Параметры запроса

```json
{
  "code": "Код для проверки безопасности",
  "check_types": ["sql_injection", "dangerous_operations", "data_access"],
  "severity_threshold": "medium",
  "compliance_rules": ["OWASP_1C", "CIS_1C"]
}
```

#### Типы проверок безопасности

- `sql_injection` - SQL-инъекции
- `dangerous_operations` - Опасные операции (удаление, изменение)
- `data_access` - Доступ к данным
- `file_operations` - Операции с файлами
- `external_calls` - Внешние вызовы
- `privilege_escalation` - Эскалация привилегий

#### Уровни серьезности

- `low` - Низкий риск
- `medium` - Средний риск
- `high` - Высокий риск
- `critical` - Критический риск

#### Пример запроса

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "method": "tools/call",
  "params": {
    "name": "security_check",
    "arguments": {
      "code": "Процедура ОпаснаяОперация() Экспорт\n    ПользовательскийЗапрос = \"'; DROP TABLE users; --\";\n    Запрос = Новый Запрос;\n    Запрос.Текст = \"ВЫБРАТЬ * ИЗ таблица WHERE поле = '\" + ПользовательскийЗапрос + \"'\";\nКонецПроцедуры",
      "check_types": ["sql_injection", "dangerous_operations"],
      "severity_threshold": "medium"
    }
  }
}
```

#### Ответ

```json
{
  "jsonrpc": "2.0",
  "id": 3,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Обнаружено 2 угрозы безопасности. Требуется немедленное исправление."
      }
    ],
    "security_report": {
      "threats_detected": 2,
      "overall_risk": "high",
      "threats": [
        {
          "type": "sql_injection",
          "severity": "critical",
          "description": "Обнаружена SQL-инъекция в строке 4",
          "line": 4,
          "recommendation": "Использовать параметризованные запросы"
        },
        {
          "type": "dangerous_operations",
          "severity": "medium",
          "description": "Потенциально опасная конкатенация строк",
          "line": 4,
          "recommendation": "Валидировать пользовательский ввод"
        }
      ],
      "compliance_status": {
        "OWASP_1C": "failed",
        "CIS_1C": "failed"
      }
    }
  }
}
```

---

### 4. optimize_prompt

Оптимизирует промпты для улучшения качества генерации кода.

**Эндпоинт:** `tools/call`  
**Название:** `optimize_prompt`

#### Параметры запроса

```json
{
  "original_prompt": "Исходный промпт для оптимизации",
  "target_improvement": 0.2,
  "optimization_type": "quality",
  "context": {
    "platform_version": "8.3.18",
    "database_type": "MS SQL"
  }
}
```

#### Типы оптимизации

- `quality` - Улучшение качества
- `clarity` - Повышение ясности
- `completeness` - Дополнение недостающих деталей
- `specificity` - Повышение специфичности

#### Пример запроса

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "method": "tools/call",
  "params": {
    "name": "optimize_prompt",
    "arguments": {
      "original_prompt": "Создать обработку для 1C. Нужно сделать анализ данных.",
      "target_improvement": 0.3,
      "optimization_type": "quality",
      "context": {
        "platform_version": "8.3.18",
        "database_type": "MS SQL",
        "object_type": "processing"
      }
    }
  }
}
```

#### Ответ

```json
{
  "jsonrpc": "2.0",
  "id": 4,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Промпт оптимизирован с улучшением на 35% по качеству."
      }
    ],
    "optimization_results": {
      "improvement_percentage": 35,
      "optimized_prompt": "Создать обработку 'АнализПродаж' для 1С:Предприятие 8.3.18. Обработка должна:\n1. Получать данные из регистра 'Продажи' за указанный период\n2. Группировать данные по номенклатуре и контрагентам\n3. Рассчитывать суммы, количество и среднюю цену\n4. Выводить результат в таблицу значений с возможностью сортировки\n5. Поддерживать фильтрацию по датам и контрагентам\n6. Включать возможность экспорта в Excel",
      "improvements": [
        "Добавлена спецификация платформы",
        "Детализированы требования к функциональности",
        "Добавлены технические детали реализации",
        "Указаны форматы вывода данных"
      ],
      "metrics": {
        "clarity_score": 0.95,
        "completeness_score": 0.88,
        "specificity_score": 0.92
      }
    }
  }
}
```

---

### 5. template_list

Возвращает список доступных шаблонов для генерации кода.

**Эндпоинт:** `tools/call`  
**Название:** `template_list`

#### Параметры запроса

```json
{
  "category": "processing|report|catalog|all",
  "filter": "search_term",
  "limit": 20
}
```

#### Пример запроса

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "method": "tools/call",
  "params": {
    "name": "template_list",
    "arguments": {
      "category": "processing",
      "filter": "анализ",
      "limit": 10
    }
  }
}
```

#### Ответ

```json
{
  "jsonrpc": "2.0",
  "id": 5,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Найдено 3 шаблона обработок по запросу 'анализ'"
      }
    ],
    "templates": [
      {
        "id": "tpl_001",
        "name": "Анализ продаж",
        "category": "processing",
        "description": "Шаблон для создания обработок анализа продаж",
        "parameters": ["object_name", "analysis_type", "period"],
        "usage_count": 45,
        "last_updated": "2025-10-28",
        "author": "AI System"
      },
      {
        "id": "tpl_002",
        "name": "Анализ остатков",
        "category": "processing",
        "description": "Шаблон для анализа товарных остатков",
        "parameters": ["warehouse", "analysis_date"],
        "usage_count": 32,
        "last_updated": "2025-10-27",
        "author": "AI System"
      }
    ],
    "categories": {
      "processing": 15,
      "report": 12,
      "catalog": 8,
      "document": 6,
      "form": 4
    },
    "total_count": 45
  }
}
```

---

## Примеры использования

### 1. Создание обработки анализа продаж

```json
{
  "jsonrpc": "2.0",
  "id": 100,
  "method": "tools/call",
  "params": {
    "name": "generate_code",
    "arguments": {
      "object_type": "processing",
      "description": "Обработка для анализа продаж товаров за период с группировкой по номенклатуре, возможностью фильтрации по датам и контрагентам, расчетом сумм и количества, и экспортом в Excel",
      "parameters": {
        "object_name": "АнализПродажТоваров",
        "description": "Анализ продаж с детализацией",
        "author": "Sales Department",
        "features": ["фильтры", "группировка", "экспорт"]
      }
    }
  }
}
```

### 2. Создание отчета по остаткам

```json
{
  "jsonrpc": "2.0",
  "id": 101,
  "method": "tools/call",
  "params": {
    "name": "generate_code",
    "arguments": {
      "object_type": "report",
      "description": "Отчет по остаткам товаров на складах с детализацией по номенклатуре, группировкой по категориям и возможностью сортировки",
      "parameters": {
        "object_name": "ОстаткиТоваровПоСкладам",
        "description": "Отчет об остатках товаров",
        "author": "Warehouse Manager",
        "features": ["детализация", "группировка", "сортировка"]
      }
    }
  }
}
```

### 3. Создание справочника контрагентов

```json
{
  "jsonrpc": "2.0",
  "id": 102,
  "method": "tools/call",
  "params": {
    "name": "generate_code",
    "arguments": {
      "object_type": "catalog",
      "description": "Справочник контрагентов с полями наименование, ИНН, КПП, адрес, телефон, email и связью с банковскими реквизитами",
      "parameters": {
        "object_name": "Контрагенты",
        "description": "Справочник контрагентов компании",
        "author": "Accountant",
        "features": ["валидация", "связи", "поиск"]
      }
    }
  }
}
```

### 4. Создание документа реализации товаров

```json
{
  "jsonrpc": "2.0",
  "id": 103,
  "method": "tools/call",
  "params": {
    "name": "generate_code",
    "arguments": {
      "object_type": "document",
      "description": "Документ 'Реализация товаров' с табличной частью товаров, расчетом сумм с учетом НДС, привязкой к контрагенту и автоматическим заполнением цен",
      "parameters": {
        "object_name": "РеализацияТоваров",
        "description": "Документ реализации товаров и услуг",
        "author": "Sales Department",
        "features": ["расчет НДС", "заполнение", "проводки"]
      }
    }
  }
}
```

### 5. Полный цикл: генерация + валидация + проверка безопасности

```json
// Шаг 1: Генерация кода
{
  "jsonrpc": "2.0",
  "id": 200,
  "method": "tools/call",
  "params": {
    "name": "generate_code",
    "arguments": {
      "object_type": "processing",
      "description": "Обработка для импорта данных из Excel с валидацией и обработкой ошибок"
    }
  }
}

// Шаг 2: Валидация (используя сгенерированный код)
{
  "jsonrpc": "2.0",
  "id": 201,
  "method": "tools/call",
  "params": {
    "name": "validate_code",
    "arguments": {
      "code": "{сгенерированный_код}",
      "validation_types": ["syntax", "semantics", "standards", "performance"],
      "strict_mode": true
    }
  }
}

// Шаг 3: Проверка безопасности
{
  "jsonrpc": "2.0",
  "id": 202,
  "method": "tools/call",
  "params": {
    "name": "security_check",
    "arguments": {
      "code": "{сгенерированный_код}",
      "check_types": ["sql_injection", "dangerous_operations", "data_access"],
      "severity_threshold": "medium"
    }
  }
}
```

---

## Руководство по конфигурации

### Базовая конфигурация

Система использует файл конфигурации `config/generation_config.yaml`:

```yaml
# Конфигурация LLM клиента
llm:
  provider: "openai|claude|local"
  model: "gpt-4|claude-3-sonnet|local-model"
  api_key: "${OPENAI_API_KEY}"
  base_url: "https://api.openai.com/v1"
  max_tokens: 4000
  temperature: 0.7
  timeout: 30

# Конфигурация шаблонов
templates:
  base_path: "./templates"
  default_category: "standard"
  auto_update: true
  validation_rules: true

# Конфигурация валидации
validation:
  strict_mode: false
  enabled_checks:
    - syntax
    - semantics
    - standards
    - performance
  custom_rules_path: "./validation/custom_rules"

# Конфигурация безопасности
security:
  severity_threshold: "medium"
  enabled_checks:
    - sql_injection
    - dangerous_operations
    - data_access
    - file_operations
  compliance_standards:
    - OWASP_1C
    - CIS_1C

# Конфигурация аудита
audit:
  enabled: true
  log_level: "INFO"
  log_path: "./logs/audit.log"
  retention_days: 90

# Конфигурация контекста
context:
  collect_metadata: true
  include_history: true
  max_context_size: 1000
  platform_info: true
```

### Переменные окружения

```bash
# Обязательные переменные
OPENAI_API_KEY=your_openai_api_key
CLAUDE_API_KEY=your_claude_api_key

# Опциональные переменные
LOG_LEVEL=INFO
TEMP_DIR=/tmp/1c_mcp
MAX_REQUEST_SIZE=10MB
```

### Запуск MCP сервера

```bash
# Способ 1: Прямой запуск
python -m src.py_server.main

# Способ 2: С Docker
docker run -p 3000:3000 -e OPENAI_API_KEY=your_key 1c-mcp-server

# Способ 3: Как системная служба
systemctl start 1c-mcp-code-generation
systemctl enable 1c-mcp-code-generation
```

### Настройка клиентов

#### Claude Desktop

```json
{
  "mcpServers": {
    "1c-code-generation": {
      "command": "python",
      "args": ["/path/to/src/py_server/main.py"],
      "env": {
        "OPENAI_API_KEY": "your_api_key"
      }
    }
  }
}
```

#### VS Code Extension

```json
{
  "1c-mcp": {
    "server": {
      "command": "python",
      "args": ["/path/to/src/py_server/main.py"],
      "env": {
        "OPENAI_API_KEY": "your_api_key"
      }
    },
    "settings": {
      "validation": {
        "auto_validate": true,
        "show_warnings": true
      },
      "generation": {
        "include_comments": true,
        "use_standards": true
      }
    }
  }
}
```

---

## Troubleshooting

### Часто встречающиеся проблемы

#### 1. Ошибки подключения к LLM

**Симптомы:**
- `LLM_GENERATION_FAILED`
- `API_KEY_INVALID`
- `RATE_LIMIT_EXCEEDED`

**Решение:**
```bash
# Проверьте правильность API ключа
echo $OPENAI_API_KEY

# Проверьте доступность API
curl -H "Authorization: Bearer $OPENAI_API_KEY" https://api.openai.com/v1/models

# Проверьте логи
tail -f logs/audit.log
```

#### 2. Проблемы с шаблонами

**Симптомы:**
- `TEMPLATE_NOT_FOUND`
- `INVALID_TEMPLATE_FORMAT`
- `TEMPLATE_RENDERING_FAILED`

**Решение:**
```bash
# Проверьте структуру шаблонов
ls -la templates/

# Валидируйте шаблон
python -c "from src.py_server.code_generation.templates.manager import TemplateManager; tm = TemplateManager(); print(tm.validate_template('template_name'))"

# Пересоздайте кеш шаблонов
rm -rf templates/cache/*
```

#### 3. Ошибки валидации

**Симптомы:**
- `VALIDATION_FAILED`
- `INVALID_SYNTAX`
- `STANDARDS_VIOLATION`

**Решение:**
```bash
# Проверьте настройки валидации
cat config/generation_config.yaml | grep validation

# Включите подробное логирование
export LOG_LEVEL=DEBUG

# Проверьте пользовательские правила
ls validation/custom_rules/
```

#### 4. Проблемы безопасности

**Симптомы:**
- `SECURITY_CHECK_FAILED`
- `THREAT_DETECTED`
- `ACCESS_DENIED`

**Решение:**
```bash
# Проверьте конфигурацию безопасности
cat config/generation_config.yaml | grep security

# Временно понизьте порог серьезности
export SECURITY_THRESHOLD=low

# Проверьте логи безопасности
grep -i security logs/audit.log
```

#### 5. Проблемы производительности

**Симптомы:**
- `REQUEST_TIMEOUT`
- `MEMORY_OVERFLOW`
- `SLOW_GENERATION`

**Решение:**
```bash
# Увеличьте таймауты
export LLM_TIMEOUT=60

# Уменьшите размер контекста
export MAX_CONTEXT_SIZE=500

# Очистите временные файлы
rm -rf /tmp/1c_mcp_*

# Проверьте использование ресурсов
top -p $(pgrep -f "python.*main.py")
```

### Диагностические команды

```bash
# Проверка состояния сервера
curl -X POST http://localhost:3000/health

# Тестирование инструментов
python -m src.py_server.test_tools

# Проверка конфигурации
python -c "from src.py_server.code_generation.engine import CodeGenerationEngine; print('Config OK' if CodeGenerationEngine.validate_config() else 'Config Error')"

# Генерация диагностического отчета
python -m src.py_server.diagnostics --full-report
```

### Логи и отладка

#### Включение подробного логирования

```yaml
# config/generation_config.yaml
logging:
  level: "DEBUG"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  handlers:
    - type: "console"
      level: "INFO"
    - type: "file"
      level: "DEBUG"
      filename: "logs/debug.log"
```

#### Анализ логов

```bash
# Просмотр последних записей
tail -f logs/audit.log

# Поиск ошибок
grep -i error logs/audit.log

# Анализ производительности
grep -i "generation_time" logs/audit.log | awk '{print $NF}' | sort -n

# Статистика использования
grep -c "tools/call" logs/audit.log
```

### Поддержка и контакты

Для получения технической поддержки:

1. **Документация:** `/docs`
2. **Логи:** `/logs`
3. **Конфигурация:** `/config`
4. **Шаблоны:** `/templates`

---

## Заключение

Данная документация покрывает все основные аспекты работы с 1C AI MCP Code Generation System. Система обеспечивает надежную, безопасную и высококачественную генерацию кода для платформы 1С:Предприятие.

### Ключевые преимущества

- ✅ **Высокое качество кода** - Генерация с использованием современных LLM
- ✅ **Полная безопасность** - Многоуровневая проверка угроз
- ✅ **Соответствие стандартам** - Автоматическая валидация по стандартам 1С
- ✅ **Гибкость настройки** - Конфигурация под любые требования
- ✅ **Простота интеграции** - Стандартный MCP протокол

### Следующие шаги

1. **Установите систему** согласно руководству по конфигурации
2. **Протестируйте основные инструменты** на простых примерах
3. **Настройте под ваши требования** через конфигурационные файлы
4. **Интегрируйте с вашими IDE** для максимального удобства

---

*Документация актуальна на 30.10.2025*  
*Версия: 1.0*  
