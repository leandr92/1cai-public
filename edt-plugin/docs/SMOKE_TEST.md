# 🧪 EDT Plugin Smoke Test

Краткий сценарий для проверки плагина 1C AI Assistant в рабочей среде 1C:EDT. Использует тестовый проект из `edt-plugin/test-fixtures`.

## Подготовка

1. Убедитесь, что backend 1C AI Stack запущен (Graph API, MCP, Neo4j, Qdrant).
2. Импортируйте демо-проект:
   - **File → New → 1C:Enterprise Project from Existing Sources**.
   - Путь: `edt-plugin/test-fixtures/SampleProject`.
3. Откройте `CommonModules/DemoModule.bsl`.
4. Откройте консоль **1C AI Assistant** (Window → Show View → Other → PDE Runtime → Console) — лог будет писаться туда.

## Шаги

| № | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Меню **1C AI Assistant → Quick Analysis** | Диалог выбора конфигурации, запуск quick-режима; в консоли появится `✅ Parsing data ready` |
| 2 | В редакторе выбрать функцию `РасчетИтоговойСуммы` → `Ctrl+Alt+Q` | Диалог метрик с реальными данными (`Called from`, `Calls`, best practices) |
| 3 | Правый клик → **1C AI Assistant → Show Call Graph** | Граф вызовов с узлами `РассчитатьНалог`, `ПолучитьКурсВалюты` |
| 4 | Меню **Run Full Analysis** (указать `ERPCPM`) | В консоли лог с шагами `Step 1/2` и `Step 2/2`; дашборд обновляется |
| 5 | Меню **Refresh Dependencies** | Консоль: `Dependencies analysis complete`; в `Analysis Dashboard` обновится блок «Граф зависимостей» |
| 6 | Меню **Update Best Practices** | Консоль: `Best practices extraction complete`; в окне быстрого анализа появятся обновлённые рекомендации |

## Проверка результатов

- **Console**: вкладка «1C AI Assistant» содержит INFO/ERROR логи запуска оркестратора.
- **Analysis Dashboard**: обновлённые показатели (`Strong Coupling`, `Best Practices`, `Isolated Modules`).
- **Output**: папка `output/analysis/` обновлена свежими JSON отчётами.

## Уборка

1. **Project → Clean…** для удаления кэшей EDT.
2. `docker compose down` для остановки backend (по желанию).
3. Очистите `logs/edt_analysis/*` и `output/analysis/*`, если требуются повторные прогоны «с нуля».

