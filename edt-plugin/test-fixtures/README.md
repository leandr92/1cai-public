# 🧪 EDT Plugin Test Fixtures

Минимальный набор файлов, чтобы быстро проверить функции плагина в 1C:EDT без доступа к "боевой" конфигурации.

## Состав

```
test-fixtures/
├── README.md
└── SampleProject/
    └── CommonModules/
        └── DemoModule.bsl
```

`DemoModule.bsl` содержит небольшие процедуры и функции с зависимостями, которых достаточно, чтобы отработали:

- **Quick Analysis** (`Ctrl+Alt+Q`) — рассчитывает метрики и зависимостей по выбранной функции.
- **Show Call Graph** — строит граф вызовов (включая искусственно добавленные вызовы).
- **Update Best Practices / Refresh Dependencies** — обновляют анализ по локальному модулю.

## Как использовать

1. Создайте отдельное рабочее пространство 1C:EDT (рекомендуется `~/edt-workspaces/ai-assistant-demo`).
2. В EDT выберите **File → New → 1C:Enterprise Project from Existing Sources**.
3. Укажите папку `edt-plugin/test-fixtures/SampleProject`.
4. После импорта откройте `CommonModules/DemoModule.bsl` и выполните:
   - Правый клик по функции → **1C AI Assistant → Quick Analysis**.
   - **Ctrl+Alt+Q** прямо из редактора.
   - Меню **1C AI Assistant → Refresh Dependencies** и **Update Best Practices**.
5. После выполнения откройте **Analysis Dashboard** — увидите обновлённые данные для тестовой конфигурации.

## Автоматический smoke-check (полуручной)

1. Выполните `make train-ml-demo` (обновит анализ, если backend запущен).
2. В EDT:
   - `Quick Analysis` на функции `РасчётИтоговойСуммы`.
   - `Show Call Graph` на `ПолучитьКурсВалюты`.
3. Убедитесь, что в консоли **1C AI Assistant** появились строки `✅` для каждого шага оркестратора.

> ℹ️ Файлы предназначены для разработческой проверки — данные вымышленные, завязок на реальную конфигурацию нет.

