version: '3.8'

# ═══════════════════════════════════════════════════════════════════════════
# Stage 1: Foundation - Neo4j, Qdrant, Elasticsearch, Ollama
# Extends base docker-compose.yml
# ═══════════════════════════════════════════════════════════════════════════
#
# ⚠️ ОПЦИОНАЛЬНЫЕ КОМПОНЕНТЫ - не обязательны для MVP
#
# 💡 TIP: Используйте этот файл только если вам нужны расширенные функции:
#         - Граф зависимостей (Neo4j)
#         - Семантический поиск (Qdrant)
#         - Полнотекстовый поиск (Elasticsearch)
#         - Локальные LLM (Ollama)
#
# Включено:
#   🟡 Neo4j - граф зависимостей (опционально, 30% используется)
#   🟡 Qdrant - векторный поиск (опционально, 30% используется)
#   ❌ Elasticsearch - полнотекстовый поиск (можно убрать, PostgreSQL FTS достаточно)
#   🟡 Ollama - локальные LLM (опционально)
#   🟡 Telegram Bot (опционально)
#
# Требования:
#   - RAM: ~12 GB (с Elasticsearch) или ~8 GB (без Elasticsearch)
#   - Время старта: ~5 минут (первый запуск)
#
# Запуск:
#   docker-compose -f docker-compose.yml -f docker-compose.stage1.yml up -d
#
# Запуск без Elasticsearch (рекомендуется для MVP):
#   docker-compose -f docker-compose.yml -f docker-compose.stage1.yml up -d \
#     postgres redis neo4j qdrant ollama
#
# ═══════════════════════════════════════════════════════════════════════════

services:
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Neo4j - Граф зависимостей 🟡 ОПЦИОНАЛЬНО (30% используется)
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  neo4j:
    image: neo4j:5.15-community
    container_name: 1c-ai-neo4j
    environment:
      NEO4J_AUTH: neo4j/${NEO4J_PASSWORD:-password}
      NEO4J_PLUGINS: '["apoc", "graph-data-science"]'
      NEO4J_dbms_memory_pagecache_size: 2G
      NEO4J_dbms_memory_heap_initial__size: 2G
      NEO4J_dbms_memory_heap_max__size: 4G
      NEO4J_apoc_export_file_enabled: "true"
      NEO4J_apoc_import_file_enabled: "true"
      NEO4J_apoc_import_file_use__neo4j__config: "true"
    ports:
      - "7474:7474"  # HTTP
      - "7687:7687"  # Bolt
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
      - neo4j_plugins:/plugins
      - ./neo4j/import:/var/lib/neo4j/import
    networks:
      - 1c-ai-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "cypher-shell -u neo4j -p ${NEO4J_PASSWORD:-password} 'RETURN 1'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Qdrant - Векторный поиск 🟡 ОПЦИОНАЛЬНО (30% используется)
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  qdrant:
    image: qdrant/qdrant:v1.7.4
    container_name: 1c-ai-qdrant
    ports:
      - "6333:6333"  # REST API
      - "6334:6334"  # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    environment:
      QDRANT__SERVICE__GRPC_PORT: 6334
    networks:
      - 1c-ai-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:6333/readyz || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Elasticsearch - Полнотекстовый поиск ❌ НЕ РЕКОМЕНДУЕТСЯ для MVP
  # 💡 Альтернатива: PostgreSQL Full-Text Search достаточно для старта
  # 💾 Требует: ~2-3 GB RAM
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  elasticsearch:
    image: elasticsearch:8.11.3
    container_name: 1c-ai-elasticsearch
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
      - xpack.security.enabled=false
      - xpack.security.http.ssl.enabled=false
      - cluster.name=1c-ai-cluster
      - node.name=1c-ai-node
    ports:
      - "9200:9200"  # REST API
      - "9300:9300"  # Transport
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    networks:
      - 1c-ai-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Kibana - UI для Elasticsearch ❌ НЕ РЕКОМЕНДУЕТСЯ для MVP
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  kibana:
    image: kibana:8.11.3
    container_name: 1c-ai-kibana
    environment:
      ELASTICSEARCH_HOSTS: http://elasticsearch:9200
      ELASTICSEARCH_USERNAME: elastic
      SERVER_NAME: kibana.1c-ai.local
    ports:
      - "5601:5601"
    networks:
      - 1c-ai-network
    depends_on:
      elasticsearch:
        condition: service_healthy
    profiles:
      - dev
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5601/api/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Ollama - Локальные LLM модели 🟡 ОПЦИОНАЛЬНО (для Qwen3-Coder)
  # 💡 Требует GPU для нормальной работы
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ollama:
    image: ollama/ollama:latest
    container_name: 1c-ai-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    networks:
      - 1c-ai-network
    restart: unless-stopped
    environment:
      - OLLAMA_KEEP_ALIVE=24h
    healthcheck:
      test: ["CMD-SHELL", "ollama list || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Telegram Bot - Пользовательский интерфейс ✅ РЕКОМЕНДУЕТСЯ
  # 💡 Можно запускать отдельно: python src/telegram/bot_minimal.py
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  telegram-bot:
    build:
      context: .
      dockerfile: docker/Dockerfile.telegram
    container_name: 1c-ai-telegram-bot
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_ADMIN_IDS=${TELEGRAM_ADMIN_IDS:-}
      - TELEGRAM_PREMIUM_IDS=${TELEGRAM_PREMIUM_IDS:-}
      - TELEGRAM_RATE_LIMIT_MIN=${TELEGRAM_RATE_LIMIT_MIN:-10}
      - TELEGRAM_RATE_LIMIT_DAY=${TELEGRAM_RATE_LIMIT_DAY:-100}
      # Database connections
      - POSTGRES_HOST=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DB=knowledge_base
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-changeme}
      - NEO4J_URI=bolt://neo4j:7687
      - NEO4J_USER=neo4j
      - NEO4J_PASSWORD=${NEO4J_PASSWORD:-password}
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
      - OLLAMA_HOST=ollama
      - OLLAMA_PORT=11434
    networks:
      - 1c-ai-network
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      neo4j:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    profiles:
      - telegram

  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # MinIO - S3 совместимое хранилище 🟡 ОПЦИОНАЛЬНО
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  minio:
    image: minio/minio:RELEASE.2024-10-13T22-08-55Z
    container_name: 1c-ai-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - 1c-ai-network
    restart: unless-stopped

  minio-setup:
    image: minio/mc:RELEASE.2024-10-14T21-43-55Z
    container_name: 1c-ai-minio-setup
    depends_on:
      - minio
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
      MINIO_DEFAULT_BUCKET: ${MINIO_DEFAULT_BUCKET:-onecai-artifacts}
    entrypoint: ["/bin/sh", "-c"]
    command: >
      mc alias set local http://minio:9000 "$MINIO_ROOT_USER" "$MINIO_ROOT_PASSWORD" &&
      mc mb -p local/"$MINIO_DEFAULT_BUCKET" &&
      mc anonymous set download local/"$MINIO_DEFAULT_BUCKET"
    networks:
      - 1c-ai-network
    restart: "no"
    profiles:
      - minio-init

volumes:
  neo4j_data:
    name: 1c-ai-neo4j-data
  neo4j_logs:
    name: 1c-ai-neo4j-logs
  neo4j_plugins:
    name: 1c-ai-neo4j-plugins
  qdrant_storage:
    name: 1c-ai-qdrant-storage
  elasticsearch_data:
    name: 1c-ai-elasticsearch-data
  ollama_models:
    name: 1c-ai-ollama-models
  minio_data:
    name: 1c-ai-minio-data

networks:
  1c-ai-network:
    external: true




