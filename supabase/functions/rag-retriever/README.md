# RAG Retriever Edge Function

Edge Function для семантического поиска в базе знаний its.1c.ru с поддержкой RAG (Retrieval-Augmented Generation).

## Возможности

- 🔍 **Семантический поиск** через векторные эмбеддинги
- 🔄 **Гибридный поиск** (векторный + полнотекстовый)
- 📊 **Интеллектуальное ранжирование** результатов
- 🏷️ **Фильтрация по категориям**
- 🤖 **Формирование контекста для AI**
- 📈 **Настраиваемые лимиты результатов**
- 🔐 **CORS заголовки** для веб-интеграции

## API Endpoints

### POST /functions/v1/rag-retriever

Основной endpoint для поиска документов.

### Параметры запроса

```json
{
  "query": "Как создать справочник в 1С Предприятие?",
  "limit": 10,
  "categories": ["официальная_документация", "видеоуроки"],
  "useHybridSearch": true,
  "useSemanticOnly": false,
  "minRelevanceScore": 0.7,
  "includeContent": true,
  "language": "ru"
}
```

#### Обязательные параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| `query` | string | Поисковый запрос |

#### Опциональные параметры

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `limit` | number | 10 | Максимальное количество результатов |
| `categories` | string[] | [] | Фильтр по категориям документации |
| `useHybridSearch` | boolean | true | Использовать гибридный поиск |
| `useSemanticOnly` | boolean | false | Только семантический поиск |
| `minRelevanceScore` | number | 0.7 | Минимальный порог релевантности |
| `includeContent` | boolean | true | Включать контент в результаты |
| `language` | string | "ru" | Язык документов |

## Формат ответа

```json
{
  "success": true,
  "query": "Как создать справочник в 1С Предприятие?",
  "totalResults": 5,
  "results": [
    {
      "id": "uuid",
      "title": "Создание справочников",
      "content": "Текст статьи...",
      "category": "официальная_документация",
      "url": "https://its.1c.ru/docs/...",
      "relevanceScore": 0.95,
      "searchType": "semantic",
      "metadata": {
        "author": "1С",
        "publishDate": "2024-01-15",
        "tags": ["справочники", "разработка"]
      }
    }
  ],
  "context": [
    "Источник: Создание справочников (официальная_документация)\nКонтент: Текст статьи...\nURL: https://its.1c.ru/docs/...\nРелевантность: 95.0%"
  ],
  "searchParams": {
    "useHybridSearch": true,
    "useSemanticOnly": false,
    "minRelevanceScore": 0.7,
    "categories": ["официальная_документация"],
    "limit": 10,
    "language": "ru"
  },
  "timestamp": "2024-01-20T12:00:00Z"
}
```

## Типы поиска

### 1. Семантический поиск
Использует векторные эмбеддинги для поиска документов с похожим смыслом. Идеально подходит для сложных запросов на естественном языке.

### 2. Полнотекстовый поиск
Традиционный поиск по точному совпадению ключевых слов с использованием PostgreSQL full-text search.

### 3. Гибридный поиск
Комбинирует семантический и полнотекстовый поиск с интеллектуальным ранжированием результатов.

## Категории документов

Доступные категории для фильтрации:
- `официальная_документация` - Официальная документация 1С
- `видеоуроки` - Обучающие видео
- `статьи` - Статьи и руководства
- `faq` - Часто задаваемые вопросы
- `код_примеры` - Примеры кода и разработки

## Примеры использования

### Простой поиск
```bash
curl -X POST https://your-project.supabase.co/functions/v1/rag-retriever \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -H "Content-Type: application/json" \
  -d '{"query": "Как создать справочник"}'
```

### Поиск с фильтрацией по категориям
```bash
curl -X POST https://your-project.supabase.co/functions/v1/rag-retriever \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Работа с регистрами сведений",
    "categories": ["официальная_документация", "статьи"],
    "limit": 5
  }'
```

### Только семантический поиск
```bash
curl -X POST https://your-project.supabase.co/functions/v1/rag-retriever \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Как эффективно организовать учет товаров в 1С",
    "useSemanticOnly": true,
    "minRelevanceScore": 0.8
  }'
```

## Интеграция с AI

Результаты поиска включают поле `context`, которое содержит готовый контекст для передачи в AI модели:

```javascript
// Пример интеграции с OpenAI
const searchResponse = await fetch('/functions/v1/rag-retriever', {
  method: 'POST',
  headers: { /* ... */ },
  body: JSON.stringify({ query: "Как создать справочник?" })
});

const { context } = await searchResponse.json();

const aiResponse = await fetch('https://api.openai.com/v1/chat/completions', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${OPENAI_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: "gpt-4",
    messages: [
      {
        role: "system",
        content: "Ты помощник по 1С. Используй предоставленный контекст для ответа."
      },
      {
        role: "user", 
        content: `Контекст: ${context.join('\n\n')}\n\nВопрос: Как создать справочник в 1С Предприятие?`
      }
    ]
  })
});
```

## Требования

### Переменные окружения

Необходимые переменные в Supabase:
- `SUPABASE_URL` - URL проекта Supabase
- `SUPABASE_ANON_KEY` - публичный ключ для анонимного доступа
- `SUPABASE_SERVICE_ROLE_KEY` - ключ сервиса для полного доступа
- `OPENAI_API_KEY` - ключ OpenAI для создания эмбеддингов (опционально)

### База данных

Таблица `its_1c_knowledge_base` должна содержать:
- `id` - UUID primary key
- `title` - заголовок документа
- `content` - содержимое документа
- `category` - категория документа
- `url` - ссылка на оригинальный документ
- `embedding` - векторное представление (vector(1536))
- `metadata` - JSON с дополнительной информацией
- `language` - язык документа
- `created_at`, `updated_at` - временные метки

### Функции БД

Функция `match_documents()` создается автоматически при применении миграции.

## Ограничения

- Максимальный лимит результатов: 50
- Минимальная длина запроса: 1 символ
- Максимальная длина запроса: 1000 символов
- Поддерживаемые языки: ru, en

## Производительность

- Время ответа: обычно < 2 секунд
- Одновременные запросы: неограниченно
- Кэширование эмбеддингов: включено для повторяющихся запросов

## Устранение неполадок

### Часто встречающиеся ошибки

1. **Query parameter is required**
   - Убедитесь, что поле `query` присутствует в запросе

2. **Missing Supabase configuration**
   - Проверьте настройки переменных окружения в Supabase

3. **OpenAI API error**
   - Проверьте ключ OpenAI API в переменных окружения

### Логирование

Логи доступны в Supabase Dashboard → Edge Functions → rag-retriever → Logs.

## Лицензия

Проект создан для внутреннего использования в системе управления знаниями its.1c.ru.